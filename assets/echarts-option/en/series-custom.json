{
    "type": {
        "desc": "",
        "uiControl": {
            "default": "'custom'",
            "type": "string"
        }
    },
    "id": {
        "desc": "Component ID, not specified by default. If specified, it can be used to refer the component in option or API.",
        "uiControl": {
            "type": "string"
        }
    },
    "name": {
        "desc": "Series name used for displaying in [tooltip](~tooltip) and filtering with [legend](~legend), or updating data and configuration with `setOption`.",
        "uiControl": {
            "type": "string"
        }
    },
    "colorBy": {
        "desc": "> Since `v5.2.0`\n\n\n\nThe policy to take color from [option.color](~color). Valid values:\n\n+ `'series'`: assigns the colors in the palette by series, so that all data in the same series are in the same color;\n+ `'data'`: assigns colors in the palette according to data items, with each data item using a different color.",
        "uiControl": {
            "default": "'series'",
            "type": "string"
        }
    },
    "legendHoverLink": {
        "desc": "Whether to enable highlighting chart when [legend](~legend) is being hovered.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "coordinateSystem": {
        "desc": "The coordinate used in the series, whose options are:\n\n\n+ `null` or `'none'`\n\nNo coordinate.\n\n\n\n+ `'cartesian2d'`\n\nUse a two-dimensional rectangular coordinate (also known as Cartesian coordinate), with [xAxisIndex](~series-custom.xAxisIndex) and [yAxisIndex](~series-custom.yAxisIndex) to assign the corresponding axis component.\n\n\n\n+ `'polar'`\n\nUse polar coordinates, with [polarIndex](~series-custom.polarIndex) to assign the corresponding polar coordinate component.\n\n\n\n+ `'geo'`\n\nUse geographic coordinate, with [geoIndex](~series-custom.geoIndex) to assign the corresponding geographic coordinate components.\n\n\n\n\n\n+ `'none'`\n\nDo not use coordinate system.",
        "uiControl": {
            "default": "'cartesian2d'",
            "type": "string"
        }
    },
    "xAxisIndex": {
        "desc": "Index of [x axis](~xAxis) to combine with, which is  useful for multiple x axes in one chart.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "yAxisIndex": {
        "desc": "Index of [y axis](~yAxis) to combine with, which is  useful for multiple y axes in one chart.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "polarIndex": {
        "desc": "Index of [polar coordinate](~polar) to combine with, which is useful for multiple polar axes in one chart.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "geoIndex": {
        "desc": "Index of [geographic coordinate](~geo) to combine with, which is useful for multiple geographic axes in one chart.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "calendarIndex": {
        "desc": "Index of [calendar coordinates](~calendar) to combine with, which is useful for multiple calendar coordinates in one chart.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem": {
        "desc": "`custom series` requires developers to write a render logic by themselves. This render logic is called [renderItem](~series-custom.renderItem).\n\nFor example:\n\n```js\nvar option = {\n...,\nseries: [{\ntype: 'custom',\nrenderItem: function (params, api) {\nvar categoryIndex = api.value(0);\nvar start = api.coord([api.value(1), categoryIndex]);\nvar end = api.coord([api.value(2), categoryIndex]);\nvar height = api.size([0, 1])[1] * 0.6;\n\nvar rectShape = echarts.graphic.clipRectByRect({\nx: start[0],\ny: start[1] - height / 2,\nwidth: end[0] - start[0],\nheight: height\n}, {\nx: params.coordSys.x,\ny: params.coordSys.y,\nwidth: params.coordSys.width,\nheight: params.coordSys.height\n});\n\nreturn rectShape && {\ntype: 'rect',\nshape: rectShape,\nstyle: api.style()\n};\n},\ndata: data\n}]\n}\n```\n\n[renderItem](~series-custom.renderItem) will be called on each data item.\n\n[renderItem](~series-custom.renderItem) provides two parameters:\n+ [params](~series-custom.renderItem.arguments.params): provides info about the current series and data and coordinate system.\n+ [api](~series-custom.renderItem.arguments.api): includes some methods.\n\n[renderItem](~series-custom.renderItem) method should returns graphic elements definitions.See [renderItem.return](~series-custom.renderItem.return).\n\nGenerally, the main process of [renderItem](~series-custom.renderItem) is that retrieve value from data and convert them to graphic elements on the current coordinate system. Two methods in [renderItem.arguments.api](~series-custom.renderItem.arguments.api) are always used in this procedure:\n+ [api.value(...)](~series-custom.renderItem.arguments.api.value) is used to retrieve value from data. For example, `api.value(0)` retrieve the value of the first dimension in the current data item.\n+ [api.coord(...)](~series-custom.renderItem.arguments.api.coord) is used to convert data to coordinate. For example, `var point = api.coord([api.value(0), api.value(1)])` converet the data to the point on the current coordinate system.\n\nSometimes [api.size(...)](~series-custom.renderItem.arguments.api.size) method is needed, which calculates the size on the coordinate system by a given data range.\n\nMoreover, [api.style(...)](~series-custom.renderItem.arguments.api.style) method can be used to set style. It provides not only the style settings specified in [series.itemStyle](~series-custom.itemStyle), but also the result of visual mapping. This method can also be called like `api.style({fill: 'green', stroke: 'yellow'})` to override those style settings.",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments": {
        "desc": "Parameters of `renderItem`.",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.arguments.params": {
        "desc": "The first parameter of `renderItem`, including:\n\n```js\n{\ncontext: // {string} An object that developers can store something temporarily here. Life cycle: current round of rendering.\nseriesId: // {string} The id of this series.\nseriesName: // {string} The name of this series.\nseriesIndex: // {number} The index of this series.\ndataIndex: // {number} The index of this data item.\ndataIndexInside: // {number} The index of this data item in the current data window (see dataZoom).\ndataInsideLength: // {number} The count of data in the current data window (see dataZoom).\nactionType: // {string} The type of action that trigger this render.\ncoordSys: // coordSys is variable by different types of coordinate systems:\ncoordSys: {\ntype: 'cartesian2d',\nx: // {number} x of grid rect\ny: // {number} y of grid rect\nwidth: // {number} width of grid rect\nheight: // {number} height of grid rect\n},\ncoordSys: {\ntype: 'calendar',\nx: // {number} x of calendar rect\ny: // {number} y of calendar rect\nwidth: // {number} width of calendar rect\nheight: // {number} height of calendar rect\ncellWidth: // {number} calendar cellWidth\ncellHeight: // {number} calendar cellHeight\nrangeInfo: {\nstart: // date start of calendar.\nend: // date end of calendar.\nweeks: // number of weeks in calendar.\ndayCount: // day count in calendar.\n}\n},\ncoordSys: {\ntype: 'geo',\nx: // {number} x of geo rect\ny: // {number} y of geo rect\nwidth: // {number} width of geo rect\nheight: // {number} height of geo rect\nzoom: // {number} zoom ratio, 1 if no zoom, 0.5 means shrink to 50%.\n},\ncoordSys: {\ntype: 'polar',\ncx: // {number} x of polar center.\ncy: // {number} y of polar center.\nr: // {number} outer radius of polar.\nr0: // {number} inner radius of polar.\n},\ncoordSys: {\ntype: 'singleAxis',\nx: // {number} x of singleAxis rect\ny: // {number} y of singleAxis rect\nwidth: // {number} width of singleAxis rect\nheight: // {number} height of singleAxis rect\n}\n}\n```\n\nDifference between `dataIndex` and `dataIndexInside`:\n\n\n\n+ `dataIndex` is the index of a `dataItem` in the original data.\n+ `dataIndexInside` is the index of a `dataItem` in the current data window (see [dataZoom](~dataZoom).\n\n[renderItem.arguments.api](~series-custom.renderItem.arguments.api) uses `dataIndexInside` as the input parameter but not `dataIndex`, because conversion from `dataIndex` to `dataIndexInside` is time-consuming.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.arguments.api": {
        "desc": "The second parameter of `renderItem`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.arguments.api.value": {
        "desc": "Get value on the given dimension.\n\n```\n@param {number} dimension The given dimension. (index from 0).\n@param {number} [dataIndexInside] In most cases it is not necessary.\n@return {number} The value.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.coord": {
        "desc": "Convert data to coordinate.\n\n```\n@param {Array.<number>} data.\n@return {Array.<number>} Point on canvas, at least includes [x, y].\nIn polar, it also contains:\npolar: [x, y, radius, angle]\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.size": {
        "desc": "Get the size by the given data range.\n\nFor example, in `cartesian2d`, suppose calling `api.size([2, 4])` returns `[12.4, 55]`. It represents that on x axis, data range `2` corresponds to size `12.4`, and on y axis data range `4` corresponds to size `55`.\n\nIn some coordinate systems (for example, polar) or when log axis is used, the size is different in different point. So the second parameter is necessary to calculate size on the given point.\n\n```\n@param {Array.<number>} dataSize Data range.\n@param {Array.<number>} dataItem The point where the size will be calculated.\n@return {Array.<number>} The size.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.style": {
        "desc": "The method obtains style info defined in [series.itemStyle](~series-custom.itemStyle), and visual info obtained by visual mapping, and return them. Those returned info can be assigned to `style` attribute of graphic element definition directly. Developers can also override style info by calling this method like this: `api.style({fill: 'green', stroke: 'yellow'})`.\n\n```\n@param {Object} [extra] Extra style info.\n@param {number} [dataIndexInside] In most cases, this parameter is not necessary.\n@return {Object} Style info, which can be assigned to `style` attribute of graphic element definition directly.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.styleEmphasis": {
        "desc": "The method obtains style info defined in [series.itemStyle.emphasis](~series-custom.itemStyle.emphasis), and visual info obtained by visual mapping, and return them. Those returned info can be assigned to `style` attribute of graphic element definition directly. Developers can also override style info by calling this method like this: `api.style({fill: 'green', stroke: 'yellow'})`.\n\n```\n@param {Object} [extra] Extra style info.\n@param {number} [dataIndexInside] In most cases, this parameter is not necessary.\n@return {Object} Style info, which can be assigned to `style` attribute of graphic element definition directly.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.visual": {
        "desc": "Get the visual info. It is rarely be used.\n\n```\n@param {string} visualType 'color', 'symbol', 'symbolSize', ...\n@param {number} [dataIndexInside] In most cases, this parameter is not necessary.\n@return {string|number} The value of visual.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.barLayout": {
        "desc": "When `barLayout` is needed, (for example, when attaching some extra graphic elements to bar chart), this method can be used to obtain bar layout info.\n\nSee a [sample](custom-bar-trend).\n\n```\n@param {Object} opt\n@param {number} opt.count How many bars in each group.\n@param {number|string} [opt.barWidth] Width of a bar.\nCan be an absolute value like `40` or a percent value like `'60%'`.\nThe percent is based on the calculated category width.\n@param {number|string} [opt.barMaxWidth] Max width of a bar.\nCan be an absolute value like `40` or a percent value like `'60%'`.\nThe percent is based on the calculated category width.\nHas higer priority than `opt.barWidth`.\n@param {number|string} [opt.barMinWidth] Min width of a bar.\nCan be an absolute value like `40` or a percent value like `'60%'`.\nThe percent is based on the calculated category width.\nHas higer priority than `opt.barWidth`.\n@param {number} [opt.barGap] Gap of bars in a group.\n@param {number} [opt.barCategoryGap] Gap of groups.\n@return {Array.<Object>} [{\nwidth: {number} Width of a bar.\noffset: {number} Offset of a bar, based on the left most edge.\noffsetCenter: {number} bar Offset of a bar, based on the center of the bar.\n}, ...]\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.currentSeriesIndices": {
        "desc": "Obtain the current series index. Notice that the `currentSeriesIndex` is different from `seriesIndex` when legend is used to filter some series.\n\n```\n@return {number}\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.font": {
        "desc": "Obtain font string, which can be used on style setting directly.\n\n```\n@param {Object} opt\n@param {string} [opt.fontStyle]\n@param {number} [opt.fontWeight]\n@param {number} [opt.fontSize]\n@param {string} [opt.fontFamily]\n@return {string} font string.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.getWidth": {
        "desc": "```\n@return {number} Width of echarts containter.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.getHeight": {
        "desc": "```\n@return {number} Height of echarts container.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.getZr": {
        "desc": "```\n@return {module:zrender} zrender instance.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.arguments.api.getDevicePixelRatio": {
        "desc": "```\n@return {number} The current devicePixelRatio.\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return": {
        "desc": "`renderItem` should returns graphic element definitions. Each graphic element is an object. See [graphic](~graphic.elements) for detailed info. (But width\\height\\top\\bottom is not supported here)\n\nIf nothing should be rendered in this data item, just returns nothing.\n\nFor example:\n```js\n// Returns a rectangular.\n{\ntype: 'rect',\nshape: {\nx: x, y: y, width: width, height: height\n},\nstyle: api.style()\n}\n```\n\n```js\n// Returns a group of elements.\n{\ntype: 'group',\n// If diffChildrenByName is set as `true`, `child.name` will be used\n// to diff children, which improves animation transition but degrade\n// performance. The default value is `false`.\n// diffChildrenByName: true,\nchildren: [{\ntype: 'circle',\nshape: {\ncx: cx, cy: cy, r: r\n},\nstyle: api.style()\n}, {\ntype: 'line',\nshape: {\nx1: x1, y1: y1, x2: x2, y2: y2\n},\nstyle: api.style()\n}]\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group": {
        "desc": "`group` is the only type that can contain children, so that a group of elements can be positioned and transformed together.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "group",
            "type": "string"
        }
    },
    "renderItem.return_group.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_group.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_group.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_group.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_group.x), [`'y'`](~series-custom.renderItem.return_group.y), [`'scaleX'`](~series-custom.renderItem.return_group.scaleX), [`'scaleY'`](~series-custom.renderItem.return_group.scaleY), [`'rotation'`](~series-custom.renderItem.return_group.rotation), [`'originX'`](~series-custom.renderItem.return_group.originX), [`'originY'`](~series-custom.renderItem.return_group.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_group.shape), ['`style'`](~series-custom.renderItem.return_group.style), [`'extra'`](~series-custom.renderItem.return_group.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_group.x) and [`'y'`](~series-custom.renderItem.return_group.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_group.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_group.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_group.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_group.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_group.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_group.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_group.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_group.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_group.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_group.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_group.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_group.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_group.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_group.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_group.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_group.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_group.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_group.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_group.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_group.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_group.width": {
        "desc": "Specify width of this `group`.\n\nThis width is only used for the positioning of its children.\n\nWhen width is `0`, children can also be positioned according to its parent using `left: 'center'`.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.height": {
        "desc": "Specify height of this `group`.\n\nThis height is only used for the positioning of its children.\n\nWhen height is `0`, children can also be positioned according to its parent using `top: 'middle'`.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_group.diffChildrenByName": {
        "desc": "In [custom series](~series-custom), when `diffChildrenByName` is set as `true`, for each [group](~series-custom.renderItem.return_group) returned from [renderItem](~series-custom.renderItem), \"diff\" will be performed to its [children](~series-custom.renderItem.return_group.children) according to the [name](~series-custom.renderItem.return_polygon.name) attribute of each graphic elements. Here \"diff\" means that map the coming graphic elements to the existing graphic elements when repainting according to `name`, which enables the transition animation if data is modified.\n\nBut notice that the operation is performance consuming, do not use it for large data amount.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_group.children": {
        "desc": "A list of children, each item is a declaration of an element.",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_path": {
        "desc": "Use [SVG PathData](http://www.w3.org/TR/SVG/paths.html#PathData) to describe a path. Can be used to draw icons or any other shapes fitting the specified size by auto transforming.\n\nSee examples:\n[icons](custom-calendar-icon) and [shapes](custom-gantt-flight).\n\nAbout width/height, cover/contain, see\n[layout](~series-custom.renderItem.return_path.shape.layout).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "path",
            "type": "string"
        }
    },
    "renderItem.return_path.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_path.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_path.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_path.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_path.x), [`'y'`](~series-custom.renderItem.return_path.y), [`'scaleX'`](~series-custom.renderItem.return_path.scaleX), [`'scaleY'`](~series-custom.renderItem.return_path.scaleY), [`'rotation'`](~series-custom.renderItem.return_path.rotation), [`'originX'`](~series-custom.renderItem.return_path.originX), [`'originY'`](~series-custom.renderItem.return_path.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_path.shape), ['`style'`](~series-custom.renderItem.return_path.style), [`'extra'`](~series-custom.renderItem.return_path.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_path.x) and [`'y'`](~series-custom.renderItem.return_path.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_path.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_path.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_path.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_path.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_path.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_path.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_path.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_path.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_path.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_path.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_path.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_path.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_path.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_path.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_path.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_path.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_path.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.shape.pathData": {
        "desc": "[SVG PathData](http://www.w3.org/TR/SVG/paths.html#PathData).\n\nFor example, `'M0,0 L0,-20 L30,-20 C42,-20 38,-1 50,-1 L70,-1 L70,0 Z'`.\n\nIf [width](~series-custom.renderItem.return_path.shape.width), [height](~series-custom.renderItem.return_path.shape.height), [x](~series-custom.renderItem.return_path.shape.x) and [y](~series-custom.renderItem.return_path.shape.y) specified, `pathData` will be transformed to fit the defined rect. If they are not specified, do not do that.\n\n[layout](~series-custom.renderItem.return_path.shape.layout) can be used to specify the transform strategy.\n\nSee examples:\n[icons](custom-calendar-icon) and [shapes](custom-gantt-flight).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.shape.d": {
        "desc": "Alias of [pathData](~series-custom.renderItem.return_path.shape.pathData).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_path.shape.layout": {
        "desc": "If [width](~series-custom.renderItem.return_path.shape.width), [height](~series-custom.renderItem.return_path.shape.height), [x](~series-custom.renderItem.return_path.shape.x) and [y](~series-custom.renderItem.return_path.shape.y) specified, `pathData` will be transformed to fit the defined rect.\n\n`layout` can be used to specify the transform strategy.\n\nOptional value:\n+ `'center'`: Keep aspect ratio, put the path in the center of the rect, expand as far as possible but never overflow.\n+ `'cover'`: Transform the path according to the aspect ratio of the rect, fill the rect and do not overflow.",
        "uiControl": {
            "default": "'center'",
            "type": "string"
        }
    },
    "renderItem.return_path.shape.x": {
        "desc": "The x value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.shape.y": {
        "desc": "The y value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.shape.width": {
        "desc": "The width of the shape of the element.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.shape.height": {
        "desc": "The height of the shape of the element.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_path.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_path.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_path.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_path.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_path.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_path.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_path.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_path.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_path.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_path.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_path.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_path.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_path.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "image",
            "type": "string"
        }
    },
    "renderItem.return_image.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_image.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_image.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_image.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_image.x), [`'y'`](~series-custom.renderItem.return_image.y), [`'scaleX'`](~series-custom.renderItem.return_image.scaleX), [`'scaleY'`](~series-custom.renderItem.return_image.scaleY), [`'rotation'`](~series-custom.renderItem.return_image.rotation), [`'originX'`](~series-custom.renderItem.return_image.originX), [`'originY'`](~series-custom.renderItem.return_image.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_image.shape), ['`style'`](~series-custom.renderItem.return_image.style), [`'extra'`](~series-custom.renderItem.return_image.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_image.x) and [`'y'`](~series-custom.renderItem.return_image.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_image.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_image.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_image.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_image.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_image.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_image.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_image.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_image.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_image.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_image.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_image.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_image.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_image.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_image.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_image.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_image.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_image.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_image.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_image.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_image.style": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.style.image": {
        "desc": "Specify content of the image, can be a URL, or [dataURI](https://tools.ietf.org/html/rfc2397).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_image.style.x": {
        "desc": "The x value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.style.y": {
        "desc": "The y value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.style.width": {
        "desc": "The width of the shape of the element.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.style.height": {
        "desc": "The height of the shape of the element.\n\n\n\n\n\n\n\nMore attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_image.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_image.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_image.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_image.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_image.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_image.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_image.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_image.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_image.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_image.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_image.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_image.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text": {
        "desc": "Text block.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "text",
            "type": "string"
        }
    },
    "renderItem.return_text.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_text.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_text.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_text.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_text.x), [`'y'`](~series-custom.renderItem.return_text.y), [`'scaleX'`](~series-custom.renderItem.return_text.scaleX), [`'scaleY'`](~series-custom.renderItem.return_text.scaleY), [`'rotation'`](~series-custom.renderItem.return_text.rotation), [`'originX'`](~series-custom.renderItem.return_text.originX), [`'originY'`](~series-custom.renderItem.return_text.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_text.shape), ['`style'`](~series-custom.renderItem.return_text.style), [`'extra'`](~series-custom.renderItem.return_text.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_text.x) and [`'y'`](~series-custom.renderItem.return_text.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_text.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_text.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_text.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_text.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_text.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_text.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_text.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_text.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_text.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_text.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_text.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_text.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_text.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_text.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_text.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_text.style": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.style.text": {
        "desc": "Text content. `\\n` can be used as a line break.",
        "uiControl": {
            "default": "''",
            "type": "string"
        }
    },
    "renderItem.return_text.style.x": {
        "desc": "The x value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.style.y": {
        "desc": "The y value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.style.font": {
        "desc": "Font size, font type, font weight, font color, follow the form of [css font](https://developer.mozilla.org/en-US/docs/Web/CSS/font).\n\nFor example:\n```\n// size | family\nfont: '2em \"STHeiti\", sans-serif'\n\n// style | weight | size | family\nfont: 'italic bolder 16px cursive'\n\n// weight | size | family\nfont: 'bolder 2em \"Microsoft YaHei\", sans-serif'\n```",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.style.textAlign": {
        "desc": "Text horizontal alignment. Optional values: `'left'`, `'center'`, `'right'`.\n\n`'left'` means the left side of the text block is specified by the [style.x](~series-custom.renderItem.return_text.style.x), while `'right'` means the right side of the text block is specified by [style.y](~series-custom.renderItem.return_text.style.y).",
        "uiControl": {
            "default": "'left'",
            "type": "string"
        }
    },
    "renderItem.return_text.style.textVerticalAlign": {
        "desc": "Text vertical alignment. Optional values: `'top'`, `'middle'`, `'bottom'`.\n\n\n\nMore attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_text.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_text.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_text.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_text.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_text.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_text.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_text.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_text.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_text.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_text.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_text.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_text.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect": {
        "desc": "Rectangle element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "rect",
            "type": "string"
        }
    },
    "renderItem.return_rect.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_rect.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_rect.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_rect.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_rect.x), [`'y'`](~series-custom.renderItem.return_rect.y), [`'scaleX'`](~series-custom.renderItem.return_rect.scaleX), [`'scaleY'`](~series-custom.renderItem.return_rect.scaleY), [`'rotation'`](~series-custom.renderItem.return_rect.rotation), [`'originX'`](~series-custom.renderItem.return_rect.originX), [`'originY'`](~series-custom.renderItem.return_rect.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_rect.shape), ['`style'`](~series-custom.renderItem.return_rect.style), [`'extra'`](~series-custom.renderItem.return_rect.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_rect.x) and [`'y'`](~series-custom.renderItem.return_rect.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_rect.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_rect.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_rect.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_rect.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_rect.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_rect.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_rect.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_rect.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_rect.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_rect.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_rect.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_rect.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_rect.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_rect.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_rect.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_rect.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_rect.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_rect.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_rect.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_rect.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_rect.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.shape.x": {
        "desc": "The x value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.shape.y": {
        "desc": "The y value of the left-top corner of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.shape.width": {
        "desc": "The width of the shape of the element.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.shape.height": {
        "desc": "The height of the shape of the element.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_rect.shape.r": {
        "desc": "Specify border radius of the rectangular here. Generally, `r` should be `[topLeftRadius, topRightRadius, BottomRightRadius, bottomLeftRadius]`, where each item is a number.\n\nAbbreviation is enabled, for example:\n+ `r`: `1`         means `[1, 1, 1, 1]`\n+ `r`: `[1]`       means `[1, 1, 1, 1]`\n+ `r`: `[1, 2]`    means `[1, 2, 1, 2]`\n+ `r`: `[1, 2, 3]` means `[1, 2, 3, 2]`",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_rect.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_rect.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_rect.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_rect.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_rect.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_rect.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_rect.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_rect.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_rect.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_rect.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_rect.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_rect.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_rect.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle": {
        "desc": "Circle element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "circle",
            "type": "string"
        }
    },
    "renderItem.return_circle.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_circle.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_circle.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_circle.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_circle.x), [`'y'`](~series-custom.renderItem.return_circle.y), [`'scaleX'`](~series-custom.renderItem.return_circle.scaleX), [`'scaleY'`](~series-custom.renderItem.return_circle.scaleY), [`'rotation'`](~series-custom.renderItem.return_circle.rotation), [`'originX'`](~series-custom.renderItem.return_circle.originX), [`'originY'`](~series-custom.renderItem.return_circle.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_circle.shape), ['`style'`](~series-custom.renderItem.return_circle.style), [`'extra'`](~series-custom.renderItem.return_circle.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_circle.x) and [`'y'`](~series-custom.renderItem.return_circle.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_circle.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_circle.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_circle.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_circle.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_circle.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_circle.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_circle.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_circle.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_circle.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_circle.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_circle.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_circle.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_circle.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_circle.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_circle.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_circle.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_circle.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_circle.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_circle.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_circle.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_circle.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.shape.cx": {
        "desc": "The x value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.shape.cy": {
        "desc": "The y value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_circle.shape.r": {
        "desc": "Outside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_circle.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_circle.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_circle.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_circle.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_circle.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_circle.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_circle.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_circle.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_circle.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_circle.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_circle.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_circle.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring": {
        "desc": "Ring element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "ring",
            "type": "string"
        }
    },
    "renderItem.return_ring.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_ring.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_ring.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_ring.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_ring.x), [`'y'`](~series-custom.renderItem.return_ring.y), [`'scaleX'`](~series-custom.renderItem.return_ring.scaleX), [`'scaleY'`](~series-custom.renderItem.return_ring.scaleY), [`'rotation'`](~series-custom.renderItem.return_ring.rotation), [`'originX'`](~series-custom.renderItem.return_ring.originX), [`'originY'`](~series-custom.renderItem.return_ring.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_ring.shape), ['`style'`](~series-custom.renderItem.return_ring.style), [`'extra'`](~series-custom.renderItem.return_ring.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_ring.x) and [`'y'`](~series-custom.renderItem.return_ring.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_ring.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_ring.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_ring.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_ring.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_ring.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_ring.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_ring.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_ring.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_ring.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_ring.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_ring.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_ring.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_ring.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_ring.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_ring.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_ring.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_ring.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_ring.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_ring.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_ring.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_ring.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.shape.cx": {
        "desc": "The x value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.shape.cy": {
        "desc": "The y value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_ring.shape.r": {
        "desc": "Outside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.shape.r0": {
        "desc": "Inside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_ring.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_ring.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_ring.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_ring.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_ring.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_ring.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_ring.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_ring.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_ring.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_ring.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_ring.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_ring.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector": {
        "desc": "Sector element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "sector",
            "type": "string"
        }
    },
    "renderItem.return_sector.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_sector.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_sector.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_sector.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_sector.x), [`'y'`](~series-custom.renderItem.return_sector.y), [`'scaleX'`](~series-custom.renderItem.return_sector.scaleX), [`'scaleY'`](~series-custom.renderItem.return_sector.scaleY), [`'rotation'`](~series-custom.renderItem.return_sector.rotation), [`'originX'`](~series-custom.renderItem.return_sector.originX), [`'originY'`](~series-custom.renderItem.return_sector.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_sector.shape), ['`style'`](~series-custom.renderItem.return_sector.style), [`'extra'`](~series-custom.renderItem.return_sector.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_sector.x) and [`'y'`](~series-custom.renderItem.return_sector.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_sector.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_sector.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_sector.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_sector.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_sector.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_sector.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_sector.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_sector.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_sector.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_sector.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_sector.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_sector.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_sector.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_sector.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_sector.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_sector.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.shape.cx": {
        "desc": "The x value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.shape.cy": {
        "desc": "The y value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_sector.shape.r": {
        "desc": "Outside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.shape.r0": {
        "desc": "Inside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.shape.startAngle": {
        "desc": "start angle, in radian.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.shape.endAngle": {
        "desc": "end angle, in radian.",
        "uiControl": {
            "default": "Math.PI * 2",
            "type": "number"
        }
    },
    "renderItem.return_sector.shape.clockwise": {
        "desc": "Whether draw clockwise.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "renderItem.return_sector.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_sector.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_sector.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_sector.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_sector.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_sector.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_sector.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_sector.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_sector.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_sector.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_sector.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_sector.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_sector.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc": {
        "desc": "Arc element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "arc",
            "type": "string"
        }
    },
    "renderItem.return_arc.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_arc.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_arc.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_arc.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_arc.x), [`'y'`](~series-custom.renderItem.return_arc.y), [`'scaleX'`](~series-custom.renderItem.return_arc.scaleX), [`'scaleY'`](~series-custom.renderItem.return_arc.scaleY), [`'rotation'`](~series-custom.renderItem.return_arc.rotation), [`'originX'`](~series-custom.renderItem.return_arc.originX), [`'originY'`](~series-custom.renderItem.return_arc.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_arc.shape), ['`style'`](~series-custom.renderItem.return_arc.style), [`'extra'`](~series-custom.renderItem.return_arc.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_arc.x) and [`'y'`](~series-custom.renderItem.return_arc.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_arc.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_arc.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_arc.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_arc.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_arc.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_arc.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_arc.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_arc.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_arc.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_arc.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_arc.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_arc.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_arc.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_arc.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_arc.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_arc.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.shape.cx": {
        "desc": "The x value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.shape.cy": {
        "desc": "The y value of the center of the element in the coordinate system of its parent.",
        "uiControl": {
            "default": "0",
            "type": "numbr"
        }
    },
    "renderItem.return_arc.shape.r": {
        "desc": "Outside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.shape.r0": {
        "desc": "Inside radius.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.shape.startAngle": {
        "desc": "start angle, in radian.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_arc.shape.endAngle": {
        "desc": "end angle, in radian.",
        "uiControl": {
            "default": "Math.PI * 2",
            "type": "number"
        }
    },
    "renderItem.return_arc.shape.clockwise": {
        "desc": "Whether draw clockwise.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "renderItem.return_arc.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_arc.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_arc.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "\"#000\"",
            "type": "string"
        }
    },
    "renderItem.return_arc.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_arc.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_arc.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_arc.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_arc.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_arc.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_arc.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_arc.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_arc.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_arc.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon": {
        "desc": "Polygon element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "polygon",
            "type": "string"
        }
    },
    "renderItem.return_polygon.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_polygon.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_polygon.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_polygon.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_polygon.x), [`'y'`](~series-custom.renderItem.return_polygon.y), [`'scaleX'`](~series-custom.renderItem.return_polygon.scaleX), [`'scaleY'`](~series-custom.renderItem.return_polygon.scaleY), [`'rotation'`](~series-custom.renderItem.return_polygon.rotation), [`'originX'`](~series-custom.renderItem.return_polygon.originX), [`'originY'`](~series-custom.renderItem.return_polygon.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_polygon.shape), ['`style'`](~series-custom.renderItem.return_polygon.style), [`'extra'`](~series-custom.renderItem.return_polygon.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_polygon.x) and [`'y'`](~series-custom.renderItem.return_polygon.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polygon.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polygon.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_polygon.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_polygon.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_polygon.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_polygon.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_polygon.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_polygon.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_polygon.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polygon.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polygon.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polygon.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polygon.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_polygon.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polygon.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.shape.points": {
        "desc": "A list of points, which defines the shape, like `[[22, 44], [44, 55], [11, 44], ...]`.",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_polygon.shape.smooth": {
        "desc": "Whether smooth the line.\n\n+ If the value is number, bezier interpolation is used, and the value specified the level of smooth, which is in the range of `[0, 1]`.\n+ If the value is `'spline'`, Catmull-Rom spline interpolation is used.",
        "uiControl": {
            "default": "undefined",
            "type": [
                "number",
                "string"
            ]
        }
    },
    "renderItem.return_polygon.shape.smoothConstraint": {
        "desc": "Whether prevent the smooth process cause the line out of the bounding box.\n\nOnly works when `smooth` is `number` (bezier smooth).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polygon.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polygon.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_polygon.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_polygon.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polygon.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polygon.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polygon.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polygon.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polygon.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polygon.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_polygon.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_polygon.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polygon.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline": {
        "desc": "Polyline element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "polyline",
            "type": "string"
        }
    },
    "renderItem.return_polyline.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_polyline.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polyline.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polyline.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polyline.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_polyline.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_polyline.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polyline.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_polyline.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_polyline.x), [`'y'`](~series-custom.renderItem.return_polyline.y), [`'scaleX'`](~series-custom.renderItem.return_polyline.scaleX), [`'scaleY'`](~series-custom.renderItem.return_polyline.scaleY), [`'rotation'`](~series-custom.renderItem.return_polyline.rotation), [`'originX'`](~series-custom.renderItem.return_polyline.originX), [`'originY'`](~series-custom.renderItem.return_polyline.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_polyline.shape), ['`style'`](~series-custom.renderItem.return_polyline.style), [`'extra'`](~series-custom.renderItem.return_polyline.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_polyline.x) and [`'y'`](~series-custom.renderItem.return_polyline.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polyline.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polyline.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_polyline.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_polyline.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_polyline.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_polyline.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_polyline.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_polyline.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_polyline.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polyline.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polyline.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polyline.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_polyline.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_polyline.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polyline.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.shape.points": {
        "desc": "A list of points, which defines the shape, like `[[22, 44], [44, 55], [11, 44], ...]`.",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_polyline.shape.smooth": {
        "desc": "Whether smooth the line.\n\n+ If the value is number, bezier interpolation is used, and the value specified the level of smooth, which is in the range of `[0, 1]`.\n+ If the value is `'spline'`, Catmull-Rom spline interpolation is used.",
        "uiControl": {
            "default": "undefined",
            "type": [
                "number",
                "string"
            ]
        }
    },
    "renderItem.return_polyline.shape.smoothConstraint": {
        "desc": "Whether prevent the smooth process cause the line out of the bounding box.\n\nOnly works when `smooth` is `number` (bezier smooth).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_polyline.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polyline.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_polyline.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "\"#000\"",
            "type": "string"
        }
    },
    "renderItem.return_polyline.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_polyline.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polyline.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polyline.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polyline.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_polyline.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_polyline.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_polyline.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_polyline.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_polyline.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line": {
        "desc": "Line element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "line",
            "type": "string"
        }
    },
    "renderItem.return_line.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_line.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_line.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_line.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_line.x), [`'y'`](~series-custom.renderItem.return_line.y), [`'scaleX'`](~series-custom.renderItem.return_line.scaleX), [`'scaleY'`](~series-custom.renderItem.return_line.scaleY), [`'rotation'`](~series-custom.renderItem.return_line.rotation), [`'originX'`](~series-custom.renderItem.return_line.originX), [`'originY'`](~series-custom.renderItem.return_line.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_line.shape), ['`style'`](~series-custom.renderItem.return_line.style), [`'extra'`](~series-custom.renderItem.return_line.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_line.x) and [`'y'`](~series-custom.renderItem.return_line.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_line.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_line.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_line.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_line.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_line.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_line.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_line.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_line.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_line.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_line.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_line.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_line.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_line.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_line.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_line.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_line.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_line.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_line.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_line.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_line.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_line.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.shape.x1": {
        "desc": "x value of the start point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.shape.y1": {
        "desc": "y value of the start point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.shape.x2": {
        "desc": "x value of the end point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.shape.y2": {
        "desc": "y value of the end point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_line.shape.percent": {
        "desc": "Specify the percentage of drawing, useful in animation.\n\nValue range: [0, 1].",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_line.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_line.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_line.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "\"#000\"",
            "type": "string"
        }
    },
    "renderItem.return_line.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_line.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_line.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_line.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_line.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_line.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_line.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_line.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_line.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_line.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve": {
        "desc": "Quadratic bezier curve or cubic bezier curve.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.type": {
        "desc": "Must be specified when define a graphic element at the first time.\n\nOptional values:\n\n\n\n[image](~series-custom.renderItem.return_image),\n[text](~series-custom.renderItem.return_text),\n[circle](~series-custom.renderItem.return_circle),\n[sector](~series-custom.renderItem.return_sector),\n[ring](~series-custom.renderItem.return_ring),\n[polygon](~series-custom.renderItem.return_polygon),\n[polyline](~series-custom.renderItem.return_polyline),\n[rect](~series-custom.renderItem.return_rect),\n[line](~series-custom.renderItem.return_line),\n[bezierCurve](~series-custom.renderItem.return_bezierCurve),\n[arc](~series-custom.renderItem.return_arc),\n[group](~series-custom.renderItem.return_group),",
        "uiControl": {
            "default": "bezierCurve",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.id": {
        "desc": "id is used to specifying element when willing to update it.\nid can be ignored if you do not need it.",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.x": {
        "desc": "x position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.y": {
        "desc": "y position of element. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.rotation": {
        "desc": "Degree value of rotation.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.scaleX": {
        "desc": "Scale on x.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.scaleY": {
        "desc": "Scale on y.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.originX": {
        "desc": "x value of element scale and rotation origin. In pixels",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.originY": {
        "desc": "y value of element scale and rotation origin. In pixels.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\n\nThe properties can only be:\n+ Transform related properties: [`'x'`](~series-custom.renderItem.return_bezierCurve.x), [`'y'`](~series-custom.renderItem.return_bezierCurve.y), [`'scaleX'`](~series-custom.renderItem.return_bezierCurve.scaleX), [`'scaleY'`](~series-custom.renderItem.return_bezierCurve.scaleY), [`'rotation'`](~series-custom.renderItem.return_bezierCurve.rotation), [`'originX'`](~series-custom.renderItem.return_bezierCurve.originX), [`'originY'`](~series-custom.renderItem.return_bezierCurve.originY). For example:\n```js\nrenderItem: function (params, api) {\nvar coord = api.coord([api.value(0), api.value[1]);\nreturn {\ntype: 'rect',\nx: coord[0],\ny: coord[1],\nshape: {\nx: 0,\ny: 0,\nwidth: api.value(2),\nheight: 100\n},\ntransition: ['x', 'y', 'width']\n}\n}\n```\n+ Shortcut to transition all of the properties in [`'shape'`](~series-custom.renderItem.return_bezierCurve.shape), ['`style'`](~series-custom.renderItem.return_bezierCurve.style), [`'extra'`](~series-custom.renderItem.return_bezierCurve.extra). For example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3)\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```\nIt is equivalent to:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'rect',\nshape: {\nx: api.value(0),\ny: api.value(1),\nwidth: api.value(2),\nheight: api.value(3),\n// This usage can only enable part of the\n// properties transition.\ntransition: ['x', 'y', 'width', 'height']\n}\n};\n}\n```\n\nBy default, [`'x'`](~series-custom.renderItem.return_bezierCurve.x) and [`'y'`](~series-custom.renderItem.return_bezierCurve.y) are transitioned. If you want to disable the default transition, just set it as:\n```js\ntransition: [] // Am empty array.\n```\n\nSee this [example](doc-example/custom-transition-simple&edit=1&reset=1) please.",
        "uiControl": {
            "default": "['x', 'y']",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_bezierCurve.morph": {
        "desc": "Whether to enable morphing animation.\n\n**When morphing animation happen?**\n\nIf `morph` is set as `true`, the morphing animation will happen according to the following rule:\n\nEach time the render process happen, custom series will diff the old data and the new data. If a set of old data items (say, \"old set\") are value-equal to a set of new data items (say, \"new set\") in name or the specified dimensions (see parameter [transition](api.html#echartsInstance.setOption) in `setOption`), we found a pair of sets as transition candidates.\n\nThree type of transition animation can be performed between the two sets:\n+ one-to-one: if both the two sets has only one data item.\n+ one-to-many(separate): if the \"old set\" has only one data item, and the \"new set\" has more than one data items.\n+ many-to-one(combine): if the \"old set\" has more than one data items, and the \"new set\" has only one data item.\n\nNote: we do not support transition animation for the case many-to-many.\n\nThen custom series find graphic elements that has `morph: true` declared in these two sets, and map them for one to one morphing or combining or separating.\n\nSee examples: [custom-one-to-one-morph](custom-one-to-one-morph&edit=1&reset=1) and\n[custom-combine-separate-morph](custom-combine-separate-morph&edit=1&reset=1).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.z2": {
        "desc": "Define the overlap relationship between graphic elements.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.name": {
        "desc": "See [diffChildrenByName](~series-custom.renderItem.return_bezierCurve.diffChildrenByName).",
        "uiControl": {
            "default": "undefined",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.info": {
        "desc": "User defined data, can be visited in event listeners.\n\n```js\nchart.on('click', function (params) {\nconsole.log(params.info);\n});\n```",
        "uiControl": {
            "type": "*"
        }
    },
    "renderItem.return_bezierCurve.silent": {
        "desc": "Whether response to mouse events / touch events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.invisible": {
        "desc": "Whether the element is visible.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.ignore": {
        "desc": "Whether the element is totally ignored (neither render nor listen events).",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.textContent": {
        "desc": "Text block attached to an element and layout based on the element by `textConfig`.\n\n\nThe props the the same as [text](option.html#series-custom.renderItem.return_text).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.textConfig": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.textConfig.position": {
        "desc": "Position of `textContent`.\n\n+ 'left'\n+ 'right'\n+ 'top'\n+ 'bottom'\n+ 'inside'\n+ 'insideLeft'\n+ 'insideRight'\n+ 'insideTop'\n+ 'insideBottom'\n+ 'insideTopLeft'\n+ 'insideTopRight'\n+ 'insideBottomLeft'\n+ 'insideBottomRight'\n+ or like `[12, 33]`\n+ or like `['50%', '50%']`",
        "uiControl": {
            "default": "'inside'",
            "type": ""
        }
    },
    "renderItem.return_bezierCurve.textConfig.rotation": {
        "desc": "Rotation of `textContent`. In radian.",
        "uiControl": {
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.textConfig.layoutRect": {
        "desc": "Rect that `textContent` will be positioned.\nDefault to be the bounding box of host element.\n\n```js\n{\nx: number\ny: number\nwidth: number\nheight: number\n}\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.textConfig.offset": {
        "desc": "Offset of the `textContent`.\n\nThe difference of `offset` and `position` is that `offset` will be applied in the rotation.\n\n##### origin\n\n`origin` is relative to the bounding box of the host element.\nCan be percent value. Relative to the bounding box.\nIf `'center'` specified, it will be center of the bounding box.\n\nOnly available when position and rotation are both set.\n\n+ like `[12, 33]`\n+ or like `['50%', '50%']`\n+ 'center'",
        "uiControl": {
            "type": "Array"
        }
    },
    "renderItem.return_bezierCurve.textConfig.distance": {
        "desc": "Distance to the `layoutRect`。",
        "uiControl": {
            "default": "5",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.textConfig.local": {
        "desc": "If `true`, it will apply host's transform.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.textConfig.insideFill": {
        "desc": "`insideFill` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.insideFill` > \"auto-calculated-fill\"\nIn most cases, \"auto-calculated-fill\" is white.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.textConfig.insideStroke": {
        "desc": "`insideStroke` is a color string or left empty.\n\nIf a `textContent` is `\"inside\"`, its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.insideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be the same as `fill` of this element if possible, or null.",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.textConfig.outsideFill": {
        "desc": "`outsideFill` is a color string or left empty.\n\nIf a `textContent` is \"inside\", its final `fill` will be picked by this priority:\n`textContent.style.fill` > `textConfig.outsideFill` > #000",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.textConfig.outsideStroke": {
        "desc": "`outsideStroke` is a color string or left empty.\n\nIf a `textContent` is not \"inside\", its final `stroke` will be picked by this priority:\n`textContent.style.stroke` > `textConfig.outsideStroke` > \"auto-calculated-stroke\"\n\nThe rule of getting \"auto-calculated-stroke\":\n\n+ If\n+ (A) the `fill` is specified in style (either in `textContent.style` or `textContent.style.rich`)\n+ or (B) needed to draw text background (either defined in `textContent.style` or `textContent.style.rich`)\n+ \"auto-calculated-stroke\" will be null.\n+ Otherwise\n+ \"auto-calculated-stroke\" will be a near white color to distinguish \"front end\" label with messy background (like other text label, line or other graphic).",
        "uiControl": {
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.textConfig.inside": {
        "desc": "Tell echarts that I can make sure this text is inside or not.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "renderItem.return_bezierCurve.during": {
        "desc": "`during` callback enable users to set props to an element in each animation frame.\n\n```js\n(duringAPI: CustomDuringAPI) => void\n\ninterface CustomDuringAPI {\n// Set transform prop value.\n// Transform prop see `TransformProp`.\nsetTransform(key: TransformProp, val: unknown): void;\n// Get transform prop value of the current animation frame.\ngetTransform(key: TransformProp): unknown;\n// Set shape prop value.\n// Shape prop is like `{ type: 'rect', shape: { xxxProp: xxxValue } }`.\nsetShape(key: string, val: unknown): void;\n// Get shape prop value of the current animation frame.\ngetShape(key: string): unknown;\n// Set style prop value.\n// Style prop is like `{ type: 'rect', style: { xxxProp: xxxValue } }`.\nsetStyle(key: string, val: unknown): void;\n// Get style prop value of the current animation frame.\ngetStyle(key: string): unknown;\n// Set extra prop value.\n// Extra prop is like `{ type: 'rect', extra: { xxxProp: xxxValue } }`.\nsetExtra(key: string, val: unknown): void;\n// Get extra prop value of the current animation frame.\ngetExtra(key: string): unknown;\n}\n\ntype TransformProp =\n'x' | 'y' | 'scaleX' | 'scaleY' | 'originX' | 'originY' | 'rotation';\n```\n\nIn most cases users do not need this `during` callback. For example, if some props are specified in [transition](option.html#series-custom.renderItem.return_rect.transition), echarts will make interpolation for these props internally and therefore have animation based on these props automatically. But if this kind of internal interpolation does not match the user requirements of animation, users can use this `during` callback to customize them.\n\nFor example, if users are using [polygon](option.html#series-custom.renderItem.return_polygon) shape. The shape is described by [shape.points](option.html#series-custom.renderItem.return_polygon.shape.points), which is an points array like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...]\n},\n// ...\n}\n```\nIf users specify them into [transition](option.html#series-custom.renderItem.return_polygon.transition) like:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: [[12, 33], [15, 36], [19, 39], ...],\n},\ntransition: 'shape'\n// ...\n}\n```\nAlthough the points will be interpolated, the consequent animation will be like that each point runs straight to the target position, which might do not match the user requirement if some kind of track like spiral is actually needed. In this case, users can use the `during` callback like that:\n```js\n{\ntype: 'polygon',\nshape: {\npoints: calculatePoints(initialDegree),\ntransition: 'points'\n},\nextra: {\ndegree: nextDegree\n},\n// Make echarts interpolate `extra.degree` internally, based on which\n// we calculate the `points` in each animation frame.\ntransition: 'extra',\nduring: function (duringAPI) {\nvar currentDegree = duringAPI.getExtra('degree');\nduringAPI.setShape(calculatePoints(currentDegree));\n}\n// ...\n}\n```\n\nSee this example [example](custom-spiral-race&edit=1&reset=1).",
        "uiControl": {
            "type": "Function"
        }
    },
    "renderItem.return_bezierCurve.extra": {
        "desc": "Users can define their own props in this `extra` field. See [during](option.html#series-custom.renderItem.return_rect.during) for the major usage of `extra`.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.extra.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `extra`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nextra: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `extra` will\n// have transition animation.\ntransition: 'extra',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_bezierCurve.shape": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.shape.x1": {
        "desc": "x value of the start point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.y1": {
        "desc": "y value of the start point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.x2": {
        "desc": "x value of the end point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.y2": {
        "desc": "y value of the end point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.cpx1": {
        "desc": "x of control point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.cpy1": {
        "desc": "y of control point.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.cpx2": {
        "desc": "x of the second control point. If specified, cubic bezier is used.\n\nIf both `cpx2` and `cpy2` are not set, quatratic bezier is used.",
        "uiControl": {
            "default": "null",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.cpy2": {
        "desc": "y of the second control point. If specified, cubic bezier is used.\n\nIf both `cpx2` and `cpy2` are not set, quatratic bezier is used.",
        "uiControl": {
            "default": "null",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.percent": {
        "desc": "Specify the percentage of drawing, useful in animation.\n\nValue range: [0, 1].",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.shape.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `shape`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nshape: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `shape` will\n// have transition animation.\ntransition: 'shape',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_bezierCurve.style": {
        "desc": "More attributes in `style` (for example, [rich text](tutorial.html#Rich%20Text)), see the `style` related attributes in [zrender/graphic/Displayable](https://ecomfe.github.io/zrender-doc/public/api.html#zrenderdisplayable).\n\nNotice, the attribute names of the `style` of graphic elements is derived from `zrender`, which may be different from the attribute names in `echarts label`, `echarts itemStyle`, etc., although they have the same meaning. For example:\n\n+ [itemStyle.color](~series-scatter.label.color) => `style.fill`\n+ [itemStyle.borderColor](~series-scatter.label.color) => `style.stroke`\n+ [label.color](~series-scatter.label.color) => `style.textFill`\n+ [label.textBorderColor](~series-scatter.label.textBorderColor) => `style.textStroke`\n+ ...",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.style.fill": {
        "desc": "Color filled in this element.",
        "uiControl": {
            "default": "'#000'",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.style.stroke": {
        "desc": "Color of stroke.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.style.lineWidth": {
        "desc": "Width of stroke.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.style.shadowBlur": {
        "desc": "Width of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.style.shadowOffsetX": {
        "desc": "X offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.style.shadowOffsetY": {
        "desc": "Y offset of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.style.shadowColor": {
        "desc": "color of shadow.",
        "uiControl": {
            "default": "undefined",
            "type": "number"
        }
    },
    "renderItem.return_bezierCurve.style.transition": {
        "desc": "Can be a single property name or an array of property names.\nEnable transition animation when the specified properties changed.\nCan only specify properties that are under this `style`.\n\nFor example:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n// This two props will perform transition animation.\ntransition: ['mmm', 'ppp']\n}\n};\n}\n```\nWe can also specify all of the properties like this:\n```js\nrenderItem: function (params, api) {\nreturn {\ntype: 'xxx',\nstyle: {\nmmm: ...,\nnnn: ...,\nppp: ...,\nqqq: ...,\n},\n// Indicate that all props in `style` will\n// have transition animation.\ntransition: 'style',\n};\n}\n```",
        "uiControl": {
            "default": "undefined",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "renderItem.return_bezierCurve.focus": {
        "desc": "> Since `v5.0.0`\n\n\n\nWhen it's highlighted, whether to fade out of other data to focus the highlighted. The following configurations are supported:\n\n+ `'none'` Do not fade out other data, it's by default.\n+ `'self'` Only focus (not fade out) the element of the currently highlighted data.\n+ `'series'` Focus on all elements of the series which the currently highlighted data belongs to.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.blurScope": {
        "desc": "> Since `v5.0.0`\n\n\n\nThe range of fade out when `focus` is enabled. Support the following configurations\n\n+ `'coordinateSystem'`\n+ `'series'`\n+ `'global'`",
        "uiControl": {
            "default": "'coordinateSystem'",
            "type": "string"
        }
    },
    "renderItem.return_bezierCurve.emphasis": {
        "desc": "Emphasis state of the element.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.emphasis.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.blur": {
        "desc": "> Since `v5.0.0`\n\n\n\nBlur state, available when `focus` is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.blur.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.select": {
        "desc": "> Since `v5.0.0`\n\n\n\nSelect state, available when [selectedMode](~series-custom.selectedMode) is set.",
        "uiControl": {
            "type": "Object"
        }
    },
    "renderItem.return_bezierCurve.select.style": {
        "desc": "Same to [style](~series-custom.renderItem.return_polygon.style).",
        "uiControl": {
            "type": "Object"
        }
    },
    "itemStyle": {
        "desc": "Graphic style of , `emphasis` is the style when it is highlighted, like being hovered by mouse, or highlighted via legend connect.",
        "uiControl": {
            "type": "Object"
        }
    },
    "itemStyle.color": {
        "desc": "color. Color is taken from [option.color Palette](~color) by default.\n\n\n\n> Supports setting as solid color using `rgb(255,255,255)`, `rgba(255,255,255,1)`, `#fff`, etc. Also supports setting as gradient color and pattern fill, see [option.color](~color) for details",
        "uiControl": {
            "default": "null",
            "type": "color"
        }
    },
    "itemStyle.borderColor": {
        "desc": "border color, whose format is similar to that of `color`.",
        "uiControl": {
            "default": "'#000'",
            "type": "color"
        }
    },
    "itemStyle.borderWidth": {
        "desc": "border width. No border when it is set to be 0.\n\n(${name} ? ${name} : \"\") + \" border width. No border when it is set to be 0.\"",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "itemStyle.borderType": {
        "desc": "border type.\n\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`borderDashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\nborderType: [5, 10],\n\nborderDashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "itemStyle.borderDashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`borderType`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "itemStyle.borderCap": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo specify how to draw the end points of the line.\nPossible values are:\n+ `'butt'`: The ends of lines are squared off at the endpoints.\n+ `'round'`: The ends of lines are rounded.\n+ `'square'`: The ends of lines are squared off by adding a box with an equal width and half the height of the line's thickness.\n\nDefault value is `'butt'`. Refer to MDN [lineCap](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineCap) for more details.",
        "uiControl": {
            "default": "'butt'",
            "type": "enum",
            "options": "'butt','round','square'"
        }
    },
    "itemStyle.borderJoin": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo determine the shape used to join two line segments where they meet.\n\nPossible values are:\n+ `'bevel'`: Fills an additional triangular area between the common endpoint of connected segments, and the separate outside rectangular corners of each segment.\n+ `'round'`: Rounds off the corners of a shape by filling an additional sector of disc centered at the common endpoint of connected segments. The radius for these rounded corners is equal to the line width.\n+ `'miter'`: Connected segments are joined by extending their outside edges to connect at a single point, with the effect of filling an additional lozenge-shaped area. This setting is affected by the\n`borderMiterLimit`\nproperty.\n\nDefault value is `'bevel'`. Refer to MDN [lineJoin](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineJoin) for more details.",
        "uiControl": {
            "default": "'bevel'",
            "type": "enum",
            "options": "'bevel','round','miter'"
        }
    },
    "itemStyle.borderMiterLimit": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the miter limit ratio. Only works when\n`borderJoin`\nis set as `miter`.\n\nDefault value is `10`. Negative、`0`、`Infinity` and `NaN` values are ignored.\n\nRefer to MDN [miterLimit](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/miterLimit) for more details.",
        "uiControl": {
            "default": "10",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "itemStyle.shadowBlur": {
        "desc": "Size of shadow blur. This attribute should be used along with `shadowColor`,`shadowOffsetX`, `shadowOffsetY` to set shadow to component.\n\nFor example:\n```js\n{\nshadowColor: 'rgba(0, 0, 0, 0.5)',\nshadowBlur: 10\n}\n```",
        "uiControl": {
            "default": "",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "itemStyle.shadowColor": {
        "desc": "Shadow color. Support same format as `color`.",
        "uiControl": {
            "default": "",
            "type": "color"
        }
    },
    "itemStyle.shadowOffsetX": {
        "desc": "Offset distance on the horizontal direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "itemStyle.shadowOffsetY": {
        "desc": "Offset distance on the vertical direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "itemStyle.opacity": {
        "desc": "Opacity of the component. Supports value from 0 to 1, and the component will not be drawn when set to 0.",
        "uiControl": {
            "default": "1",
            "type": "number",
            "min": "0",
            "max": "1",
            "step": "0.01"
        }
    },
    "itemStyle.decal": {
        "desc": "The style of the decal pattern. It works only if [aria.enabled](~aria.enabled) and [aria.decal.show](~aria.decal.show) are both set to be `true`.\n\nIf it is set to be `'none'`, no decal will be used.",
        "uiControl": {
            "type": "Object"
        }
    },
    "itemStyle.decal.symbol": {
        "desc": "The symbol type of the decal. If it is in the type of `string[]`, it means the symbols are used one by one.\n\n\n\nIcon types provided by ECharts includes\n\n\n\n`'circle'`, `'rect'`, `'roundRect'`, `'triangle'`, `'diamond'`, `'pin'`, `'arrow'`, `'none'`\n\n\n\n\n\n\n\nIt can be set to an image with `'image://url'` , in which URL is the link to an image, or `dataURI` of an image.\n\n\nAn image URL example:\n\n```\n'image://http://xxx.xxx.xxx/a/b.png'\n```\n\nA `dataURI` example:\n\n```\n'image://data:image/gif;base64,'\n```\n\n\n\nIcons can be set to arbitrary vector path via `'path://'` in ECharts. As compared with a raster image, vector paths prevent jagging and blurring when scaled, and have better control over changing colors. The size of the vector icon will be adapted automatically. Refer to [SVG PathData](http://www.w3.org/TR/SVG/paths.html#PathData) for more information about the format of the path. You may export vector paths from tools like Adobe\n\nFor example:\n\n```\n'path://M30.9,53.2C16.8,53.2,5.3,41.7,5.3,27.6S16.8,2,30.9,2C45,2,56.4,13.5,56.4,27.6S45,53.2,30.9,53.2z M30.9,3.5C17.6,3.5,6.8,14.4,6.8,27.6c0,13.3,10.8,24.1,24.101,24.1C44.2,51.7,55,40.9,55,27.6C54.9,14.4,44.1,3.5,30.9,3.5z M36.9,35.8c0,0.601-0.4,1-0.9,1h-1.3c-0.5,0-0.9-0.399-0.9-1V19.5c0-0.6,0.4-1,0.9-1H36c0.5,0,0.9,0.4,0.9,1V35.8z M27.8,35.8 c0,0.601-0.4,1-0.9,1h-1.3c-0.5,0-0.9-0.399-0.9-1V19.5c0-0.6,0.4-1,0.9-1H27c0.5,0,0.9,0.4,0.9,1L27.8,35.8L27.8,35.8z'\n```",
        "uiControl": {
            "default": "'rect'",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "itemStyle.decal.symbolSize": {
        "desc": "Range of values: `0` to `1`, representing the size of symbol relative to decal.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "itemStyle.decal.symbolKeepAspect": {
        "desc": "Whether or not to keep the aspect ratio of the pattern.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "itemStyle.decal.color": {
        "desc": "For the color of the decal pattern, it is recommended to use a translucent color, which can be superimposed on the color of the series itself.",
        "uiControl": {
            "default": "'rgba(0, 0, 0, 0.2)'",
            "type": "string"
        }
    },
    "itemStyle.decal.backgroundColor": {
        "desc": "The background color of the decal will be over the color of the series itself, under the decal pattern.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "itemStyle.decal.dashArrayX": {
        "desc": "The basic pattern of the decal pattern is an infinite loop in the form of `Pattern - Blank - Pattern - Blank - Pattern - Blank` both horizontally and vertically, respectively. By setting the length of each pattern and blank, complex pattern effects can be achieved.\n\n`dashArrayX` controls the horizontal pattern pattern. When its value is of type `number` or `number[]`, it is similar to [SVG stroke-dasharray](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray).\n\n- If it is of type `number`, it means that the pattern and the blank space are of this value respectively. For example, `5` means the pattern with width 5 is displayed first, then 5 pixels empty, then the pattern with width 5 is displayed...\n\n- In the case of `number[]` type, it means that the pattern and empty space are loops of an array of values. For example: `[5, 10, 2, 6]` means the pattern is 5 pixels wide, then 10 pixels empty, then the pattern is 2 pixels wide, then 6 pixels empty, then the pattern is 5 pixels wide...\n\n- If of type `(number | number[])[]`, it means that each row is a loop with an array of values for the pattern and blank space. For example: `[10, [2, 5]]` means that the first line will be 10 pixels by 10 pixels and empty space, the second line will be 2 pixels by 2 pixels and empty space, and the third line will be 10 pixels by 10 pixels and empty space...\n\nThis interface can be better understood with the following examples.\n\n~[700x300](doc-example/aria-decal&edit=1&reset=1)",
        "uiControl": {
            "default": "5",
            "type": [
                "number",
                "Array"
            ]
        }
    },
    "itemStyle.decal.dashArrayY": {
        "desc": "The basic pattern of the decal pattern is an infinite loop in the form of `Pattern - Blank - Pattern - Blank - Pattern - Blank` both horizontally and vertically, respectively. By setting the length of each pattern and blank, complex pattern effects can be achieved.\n\n`dashArrayY` controls the horizontal pattern pattern. Similar to [SVG stroke-dasharray](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray).\n\n- If it is a `number` type, it means the pattern and the blank are each of this value. For example, `5` means that the pattern with a height of 5 is displayed first, then 5 pixels empty, then the pattern with a height of 5 is displayed...\n\n- In the case of `number[]` type, it means that the pattern and empty space are loops of sequential array values. For example: `[5, 10, 2, 6]` means the pattern is 5 pixels high, then 10 pixels empty, then the pattern is 2 pixels high, then 6 pixels empty, then the pattern is 5 pixels high...\n\nThis interface can be better understood with the following examples.\n\n~[700x300](doc-example/aria-decal&edit=1&reset=1)",
        "uiControl": {
            "default": "5",
            "type": [
                "number",
                "Array"
            ]
        }
    },
    "itemStyle.decal.rotation": {
        "desc": "The overall rotation angle (in radians) of the pattern, in the range from `-Math.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "itemStyle.decal.maxTileWidth": {
        "desc": "The upper limit of the width of the generated pattern before it is duplicated. Usually this value is not necessary, but you can try to increase it if you notice discontinuous seams in the pattern when it repeats.",
        "uiControl": {
            "default": "512",
            "type": "number"
        }
    },
    "itemStyle.decal.maxTileHeight": {
        "desc": "The upper limit of the height of the generated pattern before it repeats. This value is usually not necessary to set, but you can try to increase it if you find that the pattern has discontinuous seams when it is repeated.",
        "uiControl": {
            "default": "512",
            "type": "number"
        }
    },
    "labelLine": {
        "desc": "Configuration of label guide line.",
        "uiControl": {
            "type": "Object"
        }
    },
    "labelLine.show": {
        "desc": "Whether to show the label guide line.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "labelLine.showAbove": {
        "desc": "Whether to show the label guide line above the corresponding element.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "labelLine.length2": {
        "desc": "The length of the second segment of guide line.",
        "uiControl": {
            "default": "15",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "labelLine.smooth": {
        "desc": "Whether to smooth the guide line. It defaults to be `false` and can be set as `true` or the values from 0 to 1 which indicating the smoothness.",
        "uiControl": {
            "default": "false",
            "type": [
                "boolean",
                "number"
            ]
        }
    },
    "labelLine.minTurnAngle": {
        "desc": "Minimum turn angle between two segments of guide line to prevent unaesthetic display when angle is too small.\n\nCan be 0 - 180 degree.",
        "uiControl": {
            "default": "null",
            "type": "number"
        }
    },
    "labelLine.lineStyle": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "labelLine.lineStyle.color": {
        "desc": "Line color.\n\n\n\n\n\n> Supports setting as solid color using `rgb(255,255,255)`, `rgba(255,255,255,1)`, `#fff`, etc. Also supports setting as gradient color and pattern fill, see [option.color](~color) for details",
        "uiControl": {
            "default": " \"#000\"",
            "type": "color"
        }
    },
    "labelLine.lineStyle.width": {
        "desc": "line width.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "labelLine.lineStyle.type": {
        "desc": "line type.\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`dashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\ntype: [5, 10],\n\ndashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "labelLine.lineStyle.dashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`type`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "labelLine.lineStyle.cap": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo specify how to draw the end points of the line.\nPossible values are:\n+ `'butt'`: The ends of lines are squared off at the endpoints.\n+ `'round'`: The ends of lines are rounded.\n+ `'square'`: The ends of lines are squared off by adding a box with an equal width and half the height of the line's thickness.\n\nDefault value is `'butt'`. Refer to MDN [lineCap](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineCap) for more details.",
        "uiControl": {
            "default": "'butt'",
            "type": "enum",
            "options": "'butt','round','square'"
        }
    },
    "labelLine.lineStyle.join": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo determine the shape used to join two line segments where they meet.\n\nPossible values are:\n+ `'bevel'`: Fills an additional triangular area between the common endpoint of connected segments, and the separate outside rectangular corners of each segment.\n+ `'round'`: Rounds off the corners of a shape by filling an additional sector of disc centered at the common endpoint of connected segments. The radius for these rounded corners is equal to the line width.\n+ `'miter'`: Connected segments are joined by extending their outside edges to connect at a single point, with the effect of filling an additional lozenge-shaped area. This setting is affected by the\n`miterLimit`\nproperty.\n\nDefault value is `'bevel'`. Refer to MDN [lineJoin](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineJoin) for more details.",
        "uiControl": {
            "default": "'bevel'",
            "type": "enum",
            "options": "'bevel','round','miter'"
        }
    },
    "labelLine.lineStyle.miterLimit": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the miter limit ratio. Only works when\n`join`\nis set as `miter`.\n\nDefault value is `10`. Negative、`0`、`Infinity` and `NaN` values are ignored.\n\nRefer to MDN [miterLimit](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/miterLimit) for more details.",
        "uiControl": {
            "default": "10",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "labelLine.lineStyle.shadowBlur": {
        "desc": "Size of shadow blur. This attribute should be used along with `shadowColor`,`shadowOffsetX`, `shadowOffsetY` to set shadow to component.\n\nFor example:\n```js\n{\nshadowColor: 'rgba(0, 0, 0, 0.5)',\nshadowBlur: 10\n}\n```",
        "uiControl": {
            "default": "",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "labelLine.lineStyle.shadowColor": {
        "desc": "Shadow color. Support same format as `color`.",
        "uiControl": {
            "default": "",
            "type": "color"
        }
    },
    "labelLine.lineStyle.shadowOffsetX": {
        "desc": "Offset distance on the horizontal direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "labelLine.lineStyle.shadowOffsetY": {
        "desc": "Offset distance on the vertical direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "labelLine.lineStyle.opacity": {
        "desc": "Opacity of the component. Supports value from 0 to 1, and the component will not be drawn when set to 0.",
        "uiControl": {
            "default": "1",
            "type": "number",
            "min": "0",
            "max": "1",
            "step": "0.01"
        }
    },
    "labelLayout": {
        "desc": "> Since `v5.0.0`\n\n\n\nUnified layout configuration of labels.\n\nIt provide a chance to adjust the labels' `(x, y)` position, alignment based on the original layout each series provides.\n\nThis option can be a callback with following parameters.\n\n```js\n// corresponding index of data\ndataIndex: number\n// corresponding type of data. Only available in graph, in which it can be 'node' or 'edge'\ndataType?: string\n// corresponding index of series\nseriesIndex: number\n// Displayed text of label.\ntext: string\n// Bounding rectangle of label.\nlabelRect: {x: number, y: number, width: number, height: number}\n// Horizontal alignment of label.\nalign: 'left' | 'center' | 'right'\n// Vertical alignment of label.\nverticalAlign: 'top' | 'middle' | 'bottom'\n// Bounding rectangle of the element corresponding to.\nrect: {x: number, y: number, width: number, height: number}\n// Default points array of labelLine. Currently only provided in pie and funnel series.\n// It's null in other series.\nlabelLinePoints?: number[][]\n```\n\n**Example:**\n\nAlign the labels on the right. Left 10px margin to the edge.\n\n```js\nlabelLayout(params) {\nreturn {\nx: params.rect.x + 10,\ny: params.rect.y + params.rect.height / 2,\nverticalAlign: 'middle',\nalign: 'left'\n}\n}\n```\n\nSet the text size based on the size of element bounding rectangle.\n\n```js\n\nlabelLayout(params) {\nreturn {\nfontSize: Math.max(params.rect.width / 10, 5)\n};\n}\n```",
        "uiControl": {
            "type": [
                "Object",
                "Function"
            ]
        }
    },
    "labelLayout.hideOverlap": {
        "desc": "If hide the overlapped labels.\n\nThe following example shows how to hide the overlapped labels in graph automatically when zooming.\n\n~[600x400](graph-label-overlap&edit=1&reset=1)",
        "uiControl": {
            "type": "boolean"
        }
    },
    "labelLayout.moveOverlap": {
        "desc": "If move the overlapped labels to avoid overlapping.\n\nCurrently supported configurations:\n\n+ `'shiftX'` Place the labels on horizontal direction sequencely, used when aligned horizontally.\n+ `'shiftY'` Place the labels on vertial direction sequencely, used when aligned vertically.\n\nThe following example shows how to use `moverOverlap: 'shiftY'` to place the labels aligned vertically.\n\n~[600x400](scatter-label-align-right&edit=1&reset=1)",
        "uiControl": {
            "type": "string"
        }
    },
    "labelLayout.x": {
        "desc": "The x position of the label. Support absolute pixel values ​​or relative values ​​such as `'20%'`.",
        "uiControl": {
            "type": [
                "number",
                "string"
            ]
        }
    },
    "labelLayout.y": {
        "desc": "The y position of the label. Support absolute pixel values ​​or relative values ​​such as `'20%'`.",
        "uiControl": {
            "type": [
                "number",
                "string"
            ]
        }
    },
    "labelLayout.dx": {
        "desc": "The pixel offset of the label in the x direction. Can be used with `x`.",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.dy": {
        "desc": "The pixel offset of the label in the y direction. Can be used with `y`",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.rotate": {
        "desc": "Label rotation angle.",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.width": {
        "desc": "The width of displayed label. Can be used with `overflow` to constraint the label in a fixed width",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.height": {
        "desc": "The height of displayed label. Can be used with `lineOverflow` to constraint the label in a fixed height.",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.align": {
        "desc": "The horizontal alignment of the label. Can be `'left'`, `'center'`, `'right'`.",
        "uiControl": {
            "type": "string"
        }
    },
    "labelLayout.verticalAlign": {
        "desc": "The vertical alignment of the label. Can be `'top'`, `'middle'`, `'bottom'`.",
        "uiControl": {
            "type": "string"
        }
    },
    "labelLayout.fontSize": {
        "desc": "The text size of the label.",
        "uiControl": {
            "type": "number"
        }
    },
    "labelLayout.draggable": {
        "desc": "Whether to allow the user to adjust the position by dragging.",
        "uiControl": {
            "type": "boolean"
        }
    },
    "labelLayout.labelLinePoints": {
        "desc": "The array of the three points of the label guide line. The format is:\n\n```js\n[[x, y], [x, y], [x, y]]\n```\n\nIt is often used in pie charts to fine-tune the guide line that has been calculated. Usually not recommended to set it in other situations.",
        "uiControl": {
            "type": "Array"
        }
    },
    "selectedMode": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\n\nSelected mode.  It is enabled by default, and you may set it to be `false` to disabled it.\n\nBesides, it can be set to `'single'` or `'multiple'`, for single selection and multiple selections.",
        "uiControl": {
            "default": "false",
            "type": "enum",
            "options": "'false','true','single','multiple'"
        }
    },
    "dimensions": {
        "desc": "`dimensions` can be used to define dimension info for `series.data` or `dataset.source`.\n\nNotice: if [dataset](~dataset) is used, we can definite dimensions in [dataset.dimensions](~dataset.dimensions), or provide dimension names in the first column/row of [dataset.source](~dataset.source), and not need to specify `dimensions` here. But if `dimensions` is specified here, it will be used despite the dimension definitions in dataset.\n\n\nFor example:\n\n```js\noption = {\ndataset: {\nsource: [\n// 'date', 'open', 'close', 'highest', 'lowest'\n[12, 44, 55, 66, 2],\n[23, 6, 16, 23, 1],\n...\n]\n},\nseries: {\ntype: 'xxx',\n// Specify name for each dimesions, which will be displayed in tooltip.\ndimensions: ['date', 'open', 'close', 'highest', 'lowest']\n}\n}\n```\n\n```js\nseries: {\ntype: 'xxx',\ndimensions: [\nnull,                // If you do not intent to defined this dimension, use null is fine.\n{type: 'ordinal'},   // Specify type of this dimension.\n// 'ordinal' is always used in string.\n// If type is not specified, echarts will guess type by data.\n{name: 'good', type: 'number'},\n'bad'                // Equals to {name: 'bad'}.\n]\n}\n```\n\nEach data item of `dimensions` can be:\n+ `string`, for example, `'someName'`, which equals to `{name: 'someName'}`.\n+ `Object`, where the attributes can be:\n+ name: `string`.\n+ type: `string`, supports:\n+ `number`\n+ `float`, that is, [Float64Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Float64Array)\n+ `int`, that is, [Int32Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Int32Array)\n+ `ordinal`, discrete value, which represents string generally.\n+ `time`, time value, see [data](~series.data) to check the format of time value.\n+ displayName: `string`, generally used in tooltip for dimension display. If not specified, use `name` by default.\n\nWhen `dimensions` is specified, the default `tooltip` will be displayed vertically, which is better to show diemsion names. Otherwise, `tooltip` will displayed only value horizontally.",
        "uiControl": {
            "type": "Array"
        }
    },
    "encode": {
        "desc": "Define what is encoded to for each dimension of `data`. For example:\n\n```js\noption = {\ndataset: {\nsource: [\n// Each column is called a dimension.\n// There are five dimensions: 0, 1, 2, 3, 4.\n[12, 44, 55, 66, 2],\n[23, 6, 16, 23, 1],\n...\n]\n},\nseries: {\ntype: 'xxx',\nencode: {\nx: [3, 1, 5],      // Dimension 3, 1, 5 is mapped to x axis.\ny: 2,              // Dimension 2 is mapped to y axis.\ntooltip: [3, 2, 4] // Dimension 3, 2, 4 will be displayed in tooltip.\n}\n}\n}\n```\n\nWhen [dimensions](~series.dimensions) is used to defined name for a certain dimension, `encode` can refer the name directly. For example:\n\n```js\nseries: {\ntype: 'xxx',\ndimensions: ['date', 'open', 'close', 'highest', 'lowest'],\nencode: {\nx: 'date',\ny: ['open', 'close', 'highest', 'lowest']\n}\n}\n```\n\nThe basic structure of [encode](option.html#series.encode) is illustrated as follows, where the left part of colon is the name of axis like `'x'`, `'y'`, `'radius'`, `'angle'` or some special reserved names like \"tooltip\", \"itemName\" etc., and the right part of the colon is the dimension names or dimension indices (based on 0). One or more dimensions can be specified. Usually not all of mappings need to be specified, only specify needed ones.\n\nThe properties available in `encode` listed as follows:\n\n```js\n// In any of the series and coordinate systems,\n// these properties are available:\nencode: {\n// Display dimension \"product\" and \"score\" in the tooltip.\ntooltip: ['product', 'score']\n// Set the series name as the concat of the names of dimensions[1] and dimensions[3].\n// (sometimes the dimension names are too long to type in series.name manually).\nseriesName: [1, 3],\n// Using dimensions[2] as the id of each data item. This is useful when dynamically\n// update data by `chart.setOption()`, where the new and old data item can be\n// corresponded by id, by which the appropriate animation can be performed when updating.\nitemId: 2,\n// Using dimensions[3] as the name of each data item. This is useful in charts like\n// 'pie', 'funnel', where data item name can be displayed in legend.\nitemName: 3,\n// Using dimensions[4] as the groupId of each data item. groupId will be used to categorize the data. And to determine\n// How the merge and split animation are performed in the universal transition. See universalTransition option for detail.\nitemGroupId: 4\n}\n\n// These properties only work in cartesian(grid) coordinate system:\nencode: {\n// Map dimensions[1], dimensions[5] and dimension \"score\" to the X axis.\nx: [1, 5, 'score'],\n// Map dimensions[0] to the Y axis.\ny: 0\n}\n\n// These properties only work in polar coordinate system:\nencode: {\nradius: 3,\nangle: 2,\n...\n}\n\n// These properties only work in geo coordinate system:\nencode: {\nlng: 3,\nlat: 2\n}\n\n// For some type of series that are not in any coordinate system,\n// like 'pie', 'funnel' etc.:\nencode: {\nvalue: 3\n}\n```\n\nThis is an [example](dataset-encode1&edit=1&reset=1) for `encode`.\n\n\nSpecially, in [custom series(~series-custom), some property in `encode`, corresponding to axis, can be set as null to make the series not controlled by the axis, that is, the series data will not be count in the extent of the axis, and the [dataZoom](~dataZoom) on the axis will not filter the series.\n\n\n```js\nvar option = {\nxAxis: {},\nyAxis: {},\ndataZoom: [{\nxAxisIndex: 0\n}, {\nyAxisIndex: 0\n}],\nseries: {\ntype: 'custom',\nrenderItem: function (params, api) {\nreturn {\ntype: 'circle',\nshape: {\ncx: 100, // x position is always 100\ncy: api.coord([0, api.value(0)])[1],\nr: 30\n},\nstyle: {\nfill: 'blue'\n}\n};\n},\nencode: {\n// Then the series will not be controlled\n// by x axis and corresponding dataZoom.\nx: -1,\ny: 1\n},\ndata: [ ... ]\n}\n};\n```",
        "uiControl": {
            "type": "Object"
        }
    },
    "seriesLayoutBy": {
        "desc": "When [dataset](~dataset) is used, `seriesLayoutBy` specifies whether the column or the row of `dataset` is mapped to the series, namely, the series is \"layout\" on columns or rows. Optional values:\n\n+ 'column': by default, the columns of `dataset` are mapped the series. In this case, each column represents a dimension.\n+ 'row'：the rows of `dataset` are mapped to the series. In this case, each row represents a dimension.\n\nCheck this [example](dataset-series-layout-by).",
        "uiControl": {
            "default": "'column'",
            "type": "string"
        }
    },
    "datasetIndex": {
        "desc": "If [series.data](~series.data) is not specified, and [dataset](~dataset) exists, the series will use `dataset`. `datasetIndex` specifies which dataset will be used.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "dataGroupId": {
        "desc": "A groupID common to all data in the series. the groupID will be used to classify the data and determine how merge and split animations are performed in the universal transition animation.\n\nIf you are using the [dataset](~dataset) component to represent the data, it is recommended to use `encode.itemGroupID` to specify which dimension is encoded as the groupID.",
        "uiControl": {
            "type": "string"
        }
    },
    "data": {
        "desc": "Data array of series, which can be in the following forms:\n\nNotice, if no `data` specified in series, and there is [dataset](~dataset) in option, series will use the first [dataset](~dataset) as its datasource. If `data` has been specified, [dataset](~dataset) will not used.\n\n`series.datasetIndex` can be used to specify other [dataset](~dataset).\n\n\nBasically, data is represented by a two-dimension array, like the example below, where each column is named as a \"dimension\".\n```js\nseries: [{\ndata: [\n// dimX   dimY   other dimensions ...\n[  3.4,    4.5,   15,   43],\n[  4.2,    2.3,   20,   91],\n[  10.8,   9.5,   30,   18],\n[  7.2,    8.8,   18,   57]\n]\n}]\n```\n\n+ In [cartesian (grid)](~grid), \"dimX\" and \"dimY\" correspond to [xAxis](~xAxis) and [yAxis](~yAxis) respectively.\n+ In [polar](~polar) \"dimX\" and \"dimY\" correspond to [radiusAxis](~radiusAxis) 和 [angleAxis](~anbleAxis) respectively.\n+ Other dimensions are optional, which can be used in other places. For example:\n+ [visualMap](~visualMap) can map one or more dimensions to visual (color, symbol size ...).\n+ [series.symbolSize](~series.symbolSize) can be set as a callback function, where symbol size can be calculated by values of a certain dimension.\n+ Values in other dimensions can be shown by [tooltip.formatter](~tooltip.formatter) or [series.label.formatter](~series.label.formatter).\n\nEspecially, when there is one and only one category axis (axis.type is `'category'`), data can be simply be represented by a one-dimension array, like:\n```js\nxAxis: {\ndata: ['a', 'b', 'm', 'n']\n},\nseries: [{\n// Each item corresponds to each item in xAxis.data.\ndata: [23,  44,  55,  19]\n// In fact, it is the simplification of the format below:\n// data: [[0, 23], [1, 44], [2, 55], [3, 19]]\n}]\n```\n\n<br>\n**Relationship between \"value\" and [axis.type](~xAxis.type)**\n\n+ When a dimension corresponds to a value axis (axis.type is `'value'` or `'log'`):\n\nThe value can be a `number` (like `12`) (can also be a number in a `string` format, like `'12'`).\n\n+ When a dimension corresponds to a category axis (axis.type is `'category'`):\n\nThe value should be the ordinal of the axis.data (based on `0`), the string value of the axis.data. For example:\n```js\nxAxis: {\ntype: 'category',\ndata: ['Monday', 'Tuesday', 'Wednesday', 'Thursday']\n},\nyAxis: {\ntype: 'category',\ndata: ['a', 'b', 'm', 'n', 'p', 'q']\n},\nseries: [{\ndata: [\n// xAxis      yAxis\n[  0,           0,    2  ], // This point is located at xAxis: 'Monday', yAxis: 'a'.\n[  'Thursday',  2,    1  ], // This point is located at xAxis: 'Thursday', yAxis: 'm'.\n[  2,          'p',   2  ], // This point is located at xAxis: 'Wednesday', yAxis: 'p'.\n[  3,           3,    5  ]\n]\n}]\n```\nThere is an example of double category axes: [Github Punchcard](scatter-punchCard).\n\n+ When a dimension corresponds to a time axis (type is `'time'`), the value can be:\n+ a timestamp, like `1484141700832`, which represents a UTC time.\n+ a date string, in one of the formats below:\n+ a subset of [ISO 8601](http://www.ecma-international.org/ecma-262/5.1/#sec-15.9.1.15), only including (all of these are treated as local time unless timezone is specified, which is consistent with [moment](https://momentjs.com/)):\n+ only part of year/month/date/time are specified: `'2012-03'`, `'2012-03-01'`, `'2012-03-01 05'`, `'2012-03-01 05:06'`.\n+ separated by `\"T\"` or a space: `'2012-03-01T12:22:33.123'`, `'2012-03-01 12:22:33.123'`.\n+ timezone specified: `'2012-03-01T12:22:33Z'`, `'2012-03-01T12:22:33+8000'`, `'2012-03-01T12:22:33-05:00'`.\n+ other date string format (all of these are treated as local time):\n`'2012'`, `'2012-3-1'`, `'2012/3/1'`, `'2012/03/01'`,\n`'2009/6/12 2:00'`, `'2009/6/12 2:05:08'`, `'2009/6/12 2:05:08.123'`.\n+ a JavaScript Date instance created by user:\n+ Caution, when using a data string to create a Date instance, [browser differences and inconsistencies](http://dygraphs.com/date-formats.html) should be considered.\n+ For example: In chrome, `new Date('2012-01-01')` is treated as a Jan 1st 2012 in UTC, while `new Date('2012-1-1')` and `new Date('2012/01/01')` are treated as Jan 1st 2012 in local timezone. In safari `new Date('2012-1-1')` is not supported.\n+ So if you intent to perform `new Date(dateString)`, it is strongly recommended to use a time parse library (e.g., [moment](https://momentjs.com/)), or use `echarts.number.parseDate`, or check [this](http://dygraphs.com/date-formats.html).\n\n\n\n<br>\n**Customize a data item:**\n\nWhen needing to customize a data item, it can be set as an object, where property `value` reprensent real value. For example:\n```js\n[\n12,\n24,\n{\nvalue: [24, 32],\n// label style, only works in this data item.\nlabel: {},\n// item style, only works in this data item.\nitemStyle:{}\n},\n33\n]\n// Or\n[\n[12, 332],\n[24, 32],\n{\nvalue: [24, 32],\n// label style, only works in this data item.\nlabel: {},\n// item style, only works in this data item.\nitemStyle:{}\n},\n[33, 31]\n]\n```\n\n<br>\n**Empty value:**\n\n`'-'` or `null` or `undefined` or `NaN` can be used to describe that a data item does not exist (ps：*not exist* does not means its value is `0`).\n\nFor example, line chart can break when encounter an empty value, and scatter chart do not display graphic elements for empty values.\n\n<br><br>",
        "uiControl": {
            "type": "Array"
        }
    },
    "data.name": {
        "desc": "Name of data item.",
        "uiControl": {
            "type": "string"
        }
    },
    "data.value": {
        "desc": "Value of data item.",
        "uiControl": {
            "type": "number"
        }
    },
    "data.groupId": {
        "desc": "The groupID of this data item. groupID will be used to classify the data.",
        "uiControl": {
            "type": "string"
        }
    },
    "data.itemStyle": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "data.itemStyle.color": {
        "desc": "color.\n\n\n\n> Supports setting as solid color using `rgb(255,255,255)`, `rgba(255,255,255,1)`, `#fff`, etc. Also supports setting as gradient color and pattern fill, see [option.color](~color) for details",
        "uiControl": {
            "default": "null",
            "type": "color"
        }
    },
    "data.itemStyle.borderColor": {
        "desc": "border color, whose format is similar to that of `color`.",
        "uiControl": {
            "default": "'#000'",
            "type": "color"
        }
    },
    "data.itemStyle.borderWidth": {
        "desc": "border width. No border when it is set to be 0.\n\n(${name} ? ${name} : \"\") + \" border width. No border when it is set to be 0.\"",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.itemStyle.borderType": {
        "desc": "border type.\n\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`borderDashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\nborderType: [5, 10],\n\nborderDashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "data.itemStyle.borderDashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`borderType`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.itemStyle.borderCap": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo specify how to draw the end points of the line.\nPossible values are:\n+ `'butt'`: The ends of lines are squared off at the endpoints.\n+ `'round'`: The ends of lines are rounded.\n+ `'square'`: The ends of lines are squared off by adding a box with an equal width and half the height of the line's thickness.\n\nDefault value is `'butt'`. Refer to MDN [lineCap](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineCap) for more details.",
        "uiControl": {
            "default": "'butt'",
            "type": "enum",
            "options": "'butt','round','square'"
        }
    },
    "data.itemStyle.borderJoin": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo determine the shape used to join two line segments where they meet.\n\nPossible values are:\n+ `'bevel'`: Fills an additional triangular area between the common endpoint of connected segments, and the separate outside rectangular corners of each segment.\n+ `'round'`: Rounds off the corners of a shape by filling an additional sector of disc centered at the common endpoint of connected segments. The radius for these rounded corners is equal to the line width.\n+ `'miter'`: Connected segments are joined by extending their outside edges to connect at a single point, with the effect of filling an additional lozenge-shaped area. This setting is affected by the\n`borderMiterLimit`\nproperty.\n\nDefault value is `'bevel'`. Refer to MDN [lineJoin](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineJoin) for more details.",
        "uiControl": {
            "default": "'bevel'",
            "type": "enum",
            "options": "'bevel','round','miter'"
        }
    },
    "data.itemStyle.borderMiterLimit": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the miter limit ratio. Only works when\n`borderJoin`\nis set as `miter`.\n\nDefault value is `10`. Negative、`0`、`Infinity` and `NaN` values are ignored.\n\nRefer to MDN [miterLimit](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/miterLimit) for more details.",
        "uiControl": {
            "default": "10",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.itemStyle.shadowBlur": {
        "desc": "Size of shadow blur. This attribute should be used along with `shadowColor`,`shadowOffsetX`, `shadowOffsetY` to set shadow to component.\n\nFor example:\n```js\n{\nshadowColor: 'rgba(0, 0, 0, 0.5)',\nshadowBlur: 10\n}\n```",
        "uiControl": {
            "default": "",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.itemStyle.shadowColor": {
        "desc": "Shadow color. Support same format as `color`.",
        "uiControl": {
            "default": "",
            "type": "color"
        }
    },
    "data.itemStyle.shadowOffsetX": {
        "desc": "Offset distance on the horizontal direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.itemStyle.shadowOffsetY": {
        "desc": "Offset distance on the vertical direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.itemStyle.opacity": {
        "desc": "Opacity of the component. Supports value from 0 to 1, and the component will not be drawn when set to 0.",
        "uiControl": {
            "default": "1",
            "type": "number",
            "min": "0",
            "max": "1",
            "step": "0.01"
        }
    },
    "data.itemStyle.decal": {
        "desc": "The style of the decal pattern. It works only if [aria.enabled](~aria.enabled) and [aria.decal.show](~aria.decal.show) are both set to be `true`.\n\nIf it is set to be `'none'`, no decal will be used.",
        "uiControl": {
            "type": "Object"
        }
    },
    "data.itemStyle.decal.symbol": {
        "desc": "The symbol type of the decal. If it is in the type of `string[]`, it means the symbols are used one by one.\n\n\n\nIcon types provided by ECharts includes\n\n\n\n`'circle'`, `'rect'`, `'roundRect'`, `'triangle'`, `'diamond'`, `'pin'`, `'arrow'`, `'none'`\n\n\n\n\n\n\n\nIt can be set to an image with `'image://url'` , in which URL is the link to an image, or `dataURI` of an image.\n\n\nAn image URL example:\n\n```\n'image://http://xxx.xxx.xxx/a/b.png'\n```\n\nA `dataURI` example:\n\n```\n'image://data:image/gif;base64,'\n```\n\n\n\nIcons can be set to arbitrary vector path via `'path://'` in ECharts. As compared with a raster image, vector paths prevent jagging and blurring when scaled, and have better control over changing colors. The size of the vector icon will be adapted automatically. Refer to [SVG PathData](http://www.w3.org/TR/SVG/paths.html#PathData) for more information about the format of the path. You may export vector paths from tools like Adobe\n\nFor example:\n\n```\n'path://M30.9,53.2C16.8,53.2,5.3,41.7,5.3,27.6S16.8,2,30.9,2C45,2,56.4,13.5,56.4,27.6S45,53.2,30.9,53.2z M30.9,3.5C17.6,3.5,6.8,14.4,6.8,27.6c0,13.3,10.8,24.1,24.101,24.1C44.2,51.7,55,40.9,55,27.6C54.9,14.4,44.1,3.5,30.9,3.5z M36.9,35.8c0,0.601-0.4,1-0.9,1h-1.3c-0.5,0-0.9-0.399-0.9-1V19.5c0-0.6,0.4-1,0.9-1H36c0.5,0,0.9,0.4,0.9,1V35.8z M27.8,35.8 c0,0.601-0.4,1-0.9,1h-1.3c-0.5,0-0.9-0.399-0.9-1V19.5c0-0.6,0.4-1,0.9-1H27c0.5,0,0.9,0.4,0.9,1L27.8,35.8L27.8,35.8z'\n```",
        "uiControl": {
            "default": "'rect'",
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "data.itemStyle.decal.symbolSize": {
        "desc": "Range of values: `0` to `1`, representing the size of symbol relative to decal.",
        "uiControl": {
            "default": "1",
            "type": "number"
        }
    },
    "data.itemStyle.decal.symbolKeepAspect": {
        "desc": "Whether or not to keep the aspect ratio of the pattern.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "data.itemStyle.decal.color": {
        "desc": "For the color of the decal pattern, it is recommended to use a translucent color, which can be superimposed on the color of the series itself.",
        "uiControl": {
            "default": "'rgba(0, 0, 0, 0.2)'",
            "type": "string"
        }
    },
    "data.itemStyle.decal.backgroundColor": {
        "desc": "The background color of the decal will be over the color of the series itself, under the decal pattern.",
        "uiControl": {
            "default": "null",
            "type": "string"
        }
    },
    "data.itemStyle.decal.dashArrayX": {
        "desc": "The basic pattern of the decal pattern is an infinite loop in the form of `Pattern - Blank - Pattern - Blank - Pattern - Blank` both horizontally and vertically, respectively. By setting the length of each pattern and blank, complex pattern effects can be achieved.\n\n`dashArrayX` controls the horizontal pattern pattern. When its value is of type `number` or `number[]`, it is similar to [SVG stroke-dasharray](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray).\n\n- If it is of type `number`, it means that the pattern and the blank space are of this value respectively. For example, `5` means the pattern with width 5 is displayed first, then 5 pixels empty, then the pattern with width 5 is displayed...\n\n- In the case of `number[]` type, it means that the pattern and empty space are loops of an array of values. For example: `[5, 10, 2, 6]` means the pattern is 5 pixels wide, then 10 pixels empty, then the pattern is 2 pixels wide, then 6 pixels empty, then the pattern is 5 pixels wide...\n\n- If of type `(number | number[])[]`, it means that each row is a loop with an array of values for the pattern and blank space. For example: `[10, [2, 5]]` means that the first line will be 10 pixels by 10 pixels and empty space, the second line will be 2 pixels by 2 pixels and empty space, and the third line will be 10 pixels by 10 pixels and empty space...\n\nThis interface can be better understood with the following examples.\n\n~[700x300](doc-example/aria-decal&edit=1&reset=1)",
        "uiControl": {
            "default": "5",
            "type": [
                "number",
                "Array"
            ]
        }
    },
    "data.itemStyle.decal.dashArrayY": {
        "desc": "The basic pattern of the decal pattern is an infinite loop in the form of `Pattern - Blank - Pattern - Blank - Pattern - Blank` both horizontally and vertically, respectively. By setting the length of each pattern and blank, complex pattern effects can be achieved.\n\n`dashArrayY` controls the horizontal pattern pattern. Similar to [SVG stroke-dasharray](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray).\n\n- If it is a `number` type, it means the pattern and the blank are each of this value. For example, `5` means that the pattern with a height of 5 is displayed first, then 5 pixels empty, then the pattern with a height of 5 is displayed...\n\n- In the case of `number[]` type, it means that the pattern and empty space are loops of sequential array values. For example: `[5, 10, 2, 6]` means the pattern is 5 pixels high, then 10 pixels empty, then the pattern is 2 pixels high, then 6 pixels empty, then the pattern is 5 pixels high...\n\nThis interface can be better understood with the following examples.\n\n~[700x300](doc-example/aria-decal&edit=1&reset=1)",
        "uiControl": {
            "default": "5",
            "type": [
                "number",
                "Array"
            ]
        }
    },
    "data.itemStyle.decal.rotation": {
        "desc": "The overall rotation angle (in radians) of the pattern, in the range from `-Math.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "data.itemStyle.decal.maxTileWidth": {
        "desc": "The upper limit of the width of the generated pattern before it is duplicated. Usually this value is not necessary, but you can try to increase it if you notice discontinuous seams in the pattern when it repeats.",
        "uiControl": {
            "default": "512",
            "type": "number"
        }
    },
    "data.itemStyle.decal.maxTileHeight": {
        "desc": "The upper limit of the height of the generated pattern before it repeats. This value is usually not necessary to set, but you can try to increase it if you find that the pattern has discontinuous seams when it is repeated.",
        "uiControl": {
            "default": "512",
            "type": "number"
        }
    },
    "data.emphasis": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "data.emphasis.itemStyle": {
        "desc": "",
        "uiControl": {
            "type": "Object"
        }
    },
    "data.emphasis.itemStyle.color": {
        "desc": "color.\n\n\n\n> Supports setting as solid color using `rgb(255,255,255)`, `rgba(255,255,255,1)`, `#fff`, etc. Also supports setting as gradient color and pattern fill, see [option.color](~color) for details",
        "uiControl": {
            "default": "null",
            "type": "color"
        }
    },
    "data.emphasis.itemStyle.borderColor": {
        "desc": "border color, whose format is similar to that of `color`.",
        "uiControl": {
            "default": "'#000'",
            "type": "color"
        }
    },
    "data.emphasis.itemStyle.borderWidth": {
        "desc": "border width. No border when it is set to be 0.\n\n(${name} ? ${name} : \"\") + \" border width. No border when it is set to be 0.\"",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.emphasis.itemStyle.borderType": {
        "desc": "border type.\n\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`borderDashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\nborderType: [5, 10],\n\nborderDashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "data.emphasis.itemStyle.borderDashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`borderType`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.emphasis.itemStyle.borderCap": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo specify how to draw the end points of the line.\nPossible values are:\n+ `'butt'`: The ends of lines are squared off at the endpoints.\n+ `'round'`: The ends of lines are rounded.\n+ `'square'`: The ends of lines are squared off by adding a box with an equal width and half the height of the line's thickness.\n\nDefault value is `'butt'`. Refer to MDN [lineCap](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineCap) for more details.",
        "uiControl": {
            "default": "'butt'",
            "type": "enum",
            "options": "'butt','round','square'"
        }
    },
    "data.emphasis.itemStyle.borderJoin": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo determine the shape used to join two line segments where they meet.\n\nPossible values are:\n+ `'bevel'`: Fills an additional triangular area between the common endpoint of connected segments, and the separate outside rectangular corners of each segment.\n+ `'round'`: Rounds off the corners of a shape by filling an additional sector of disc centered at the common endpoint of connected segments. The radius for these rounded corners is equal to the line width.\n+ `'miter'`: Connected segments are joined by extending their outside edges to connect at a single point, with the effect of filling an additional lozenge-shaped area. This setting is affected by the\n`borderMiterLimit`\nproperty.\n\nDefault value is `'bevel'`. Refer to MDN [lineJoin](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineJoin) for more details.",
        "uiControl": {
            "default": "'bevel'",
            "type": "enum",
            "options": "'bevel','round','miter'"
        }
    },
    "data.emphasis.itemStyle.borderMiterLimit": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the miter limit ratio. Only works when\n`borderJoin`\nis set as `miter`.\n\nDefault value is `10`. Negative、`0`、`Infinity` and `NaN` values are ignored.\n\nRefer to MDN [miterLimit](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/miterLimit) for more details.",
        "uiControl": {
            "default": "10",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.emphasis.itemStyle.shadowBlur": {
        "desc": "Size of shadow blur. This attribute should be used along with `shadowColor`,`shadowOffsetX`, `shadowOffsetY` to set shadow to component.\n\nFor example:\n```js\n{\nshadowColor: 'rgba(0, 0, 0, 0.5)',\nshadowBlur: 10\n}\n```",
        "uiControl": {
            "default": "",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.emphasis.itemStyle.shadowColor": {
        "desc": "Shadow color. Support same format as `color`.",
        "uiControl": {
            "default": "",
            "type": "color"
        }
    },
    "data.emphasis.itemStyle.shadowOffsetX": {
        "desc": "Offset distance on the horizontal direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.emphasis.itemStyle.shadowOffsetY": {
        "desc": "Offset distance on the vertical direction of shadow.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.emphasis.itemStyle.opacity": {
        "desc": "Opacity of the component. Supports value from 0 to 1, and the component will not be drawn when set to 0.",
        "uiControl": {
            "default": "1",
            "type": "number",
            "min": "0",
            "max": "1",
            "step": "0.01"
        }
    },
    "data.tooltip": {
        "desc": "tooltip settings in this series data.",
        "uiControl": {
            "type": "*"
        }
    },
    "data.tooltip.position": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe position of the tooltip's floating layer, which would follow the position of mouse by default.\n\nOptions:\n\n+ `Array`\n\nDisplay the position of tooltip's floating layer through array, which supports absolute position and relative percentage.\n\nExample:\n\n```js\n// absolute position, which is 10px to the left side and 10px to the top side of the container\nposition: [10, 10]\n// relative position, in the exact center of the container\nposition: ['50%', '50%']\n```\n\n+ `Function`\n\nCallback function in the following form:\n```js\n(point: Array, params: Object|Array.<Object>, dom: HTMLDomElement, rect: Object, size: Object) => Array\n```\n\n**Parameters:**<br>\npoint: Mouse position.<br>\nparam: The same as formatter.<br>\ndom: The DOM object of tooltip.<br>\nrect: It is valid only when mouse is on graphic elements, which stands for a bounding box with `x`, `y`, `width`, and `height`.<br>\nsize: The size of dom echarts container. For example: `{contentSize: [width, height], viewSize: [width, height]}`. <br>\n\n**Return:**<br>\nReturn value is an array standing for tooltip position, which can be absolute pixels, or relative percentage.<br>\nOr can be an object, like `{left: 10, top: 30}`, or `{right: '20%', bottom: 40}`.<br>\n\nFor example:\n```js\nposition: function (point, params, dom, rect, size) {\n// fixed at top\nreturn [point[0], '10%'];\n}\n```\nOr:\n```js\nposition: function (pos, params, dom, rect, size) {\n// tooltip will be fixed on the right if mouse hovering on the left,\n// and on the left if hovering on the right.\nvar obj = {top: 60};\nobj[['left', 'right'][+(pos[0] < size.viewSize[0] / 2)]] = 5;\nreturn obj;\n}\n```\n\n+ `'inside'`\n\nCenter position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'top'`\n\nTop position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'left'`\n\nLeft position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'right'`\n\nRight position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'bottom'`\n\nBottom position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.",
        "uiControl": {
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "data.tooltip.formatter": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe content formatter of tooltip's floating layer which supports string template and callback function.\n\n**1. String template**\n\nThe template variables are `{a}`, `{b}`, `{c}`, `{d}` and `{e}`, which stands for series name, data name and data value and ect. When [trigger](~tooltip.trigger) is set to be `'axis'`, there may be data from multiple series. In this time, series index can be refered as `{a0}`, `{a1}`, or `{a2}`.\n\n`{a}`, `{b}`, `{c}`, `{d}` have different meanings for different series types:\n\n+ Line (area) charts, bar (column) charts, K charts: `{a}` for series name, `{b}` for category name, `{c}` for data value, `{d}` for none;\n\n+ Scatter (bubble) charts: `{a}` for series name, `{b}` for data name, `{c}` for data value, `{d}` for none;\n\n+ Map: `{a}` for series name, `{b}` for area name, `{c}` for merging data, `{d}` for none;\n\n+ Pie charts, gauge charts, funnel charts: `{a}` for series name, `{b}` for data item name, `{c}` for data value, `{d}` for percentage.\n\n**Example: **\n```js\nformatter: '{b0}: {c0}<br />{b1}: {c1}'\n```\n\n\n**2. Callback function**\n\nThe format of callback function:\n\n```js\n(params: Object|Array, ticket: string, callback: (ticket: string, html: string)) => string | HTMLElement | HTMLElement[]\n```\n\nThe first parameter `params` is the data that the formatter needs. Its format is shown as follows:\n\n\n\n```js\n{\ncomponentType: 'series',\n// Series type\nseriesType: string,\n// Series index in option.series\nseriesIndex: number,\n// Series name\nseriesName: string,\n// Data name, or category name\nname: string,\n// Data index in input data array\ndataIndex: number,\n// Original data as input\ndata: Object,\n// Value of data. In most series it is the same as data.\n// But in some series it is some part of the data (e.g., in map, radar)\nvalue: number|Array|Object,\n// encoding info of coordinate system\n// Key: coord, like ('x' 'y' 'radius' 'angle')\n// value: Must be an array, not null/undefined. Contain dimension indices, like:\n// {\n//     x: [2] // values on dimension index 2 are mapped to x axis.\n//     y: [0] // values on dimension index 0 are mapped to y axis.\n// }\nencode: Object,\n// dimension names list\ndimensionNames: Array<String>,\n// data dimension index, for example 0 or 1 or 2 ...\n// Only work in `radar` series.\ndimensionIndex: number,\n// Color of data\ncolor: string,\n\n{{ for:  as ,  }}{{ /for }}\n\n}\n```\n\nNote: the usage of encode and dimensionNames can be:\n\nIf data is:\n```js\ndataset: {\nsource: [\n['Matcha Latte', 43.3, 85.8, 93.7],\n['Milk Tea', 83.1, 73.4, 55.1],\n['Cheese Cocoa', 86.4, 65.2, 82.5],\n['Walnut Brownie', 72.4, 53.9, 39.1]\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.encode.y[0]]\n```\n\nIf data is:\n```js\ndataset: {\ndimensions: ['product', '2015', '2016', '2017'],\nsource: [\n{product: 'Matcha Latte', '2015': 43.3, '2016': 85.8, '2017': 93.7},\n{product: 'Milk Tea', '2015': 83.1, '2016': 73.4, '2017': 55.1},\n{product: 'Cheese Cocoa', '2015': 86.4, '2016': 65.2, '2017': 82.5},\n{product: 'Walnut Brownie', '2015': 72.4, '2016': 53.9, '2017': 39.1}\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.dimensionNames[params.encode.y[0]]]\n```\n\n\n\nWhen [trigger](~tooltip.trigger) is `'axis'`, or when tooltip is triggered by [axisPointer](~xAxis.axisPointer), `params` is the data array of multiple series. The content of each item of the array is the same as above. Besides,\n\n\n\n```js\n{\ncomponentType: 'series',\n// Series type\nseriesType: string,\n// Series index in option.series\nseriesIndex: number,\n// Series name\nseriesName: string,\n// Data name, or category name\nname: string,\n// Data index in input data array\ndataIndex: number,\n// Original data as input\ndata: Object,\n// Value of data. In most series it is the same as data.\n// But in some series it is some part of the data (e.g., in map, radar)\nvalue: number|Array|Object,\n// encoding info of coordinate system\n// Key: coord, like ('x' 'y' 'radius' 'angle')\n// value: Must be an array, not null/undefined. Contain dimension indices, like:\n// {\n//     x: [2] // values on dimension index 2 are mapped to x axis.\n//     y: [0] // values on dimension index 0 are mapped to y axis.\n// }\nencode: Object,\n// dimension names list\ndimensionNames: Array<String>,\n// data dimension index, for example 0 or 1 or 2 ...\n// Only work in `radar` series.\ndimensionIndex: number,\n// Color of data\ncolor: string,\n\n{{ for:  as ,  }}{{ /for }}\n\n}\n```\n\nNote: the usage of encode and dimensionNames can be:\n\nIf data is:\n```js\ndataset: {\nsource: [\n['Matcha Latte', 43.3, 85.8, 93.7],\n['Milk Tea', 83.1, 73.4, 55.1],\n['Cheese Cocoa', 86.4, 65.2, 82.5],\n['Walnut Brownie', 72.4, 53.9, 39.1]\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.encode.y[0]]\n```\n\nIf data is:\n```js\ndataset: {\ndimensions: ['product', '2015', '2016', '2017'],\nsource: [\n{product: 'Matcha Latte', '2015': 43.3, '2016': 85.8, '2017': 93.7},\n{product: 'Milk Tea', '2015': 83.1, '2016': 73.4, '2017': 55.1},\n{product: 'Cheese Cocoa', '2015': 86.4, '2016': 65.2, '2017': 82.5},\n{product: 'Walnut Brownie', '2015': 72.4, '2016': 53.9, '2017': 39.1}\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.dimensionNames[params.encode.y[0]]]\n```\n\n\n\n**Note: **Using array to present all the parameters in ECharts 2.x is not supported anymore.\n\nThe second parameter `ticket` is the asynchronous callback flag which should be used along with the third parameter `callback` when it is used.\n\nThe third parameter `callback` is asynchronous callback. When the content of tooltip is acquired asynchronously, `ticket` and `htm` as introduced above can be used to update tooltip with callback.\n\nExample:\n```js\nformatter: function (params, ticket, callback) {\n$.get('detail?name=' + params.name, function (content) {\ncallback(ticket, toHTML(content));\n});\nreturn 'Loading';\n}\n```",
        "uiControl": {
            "type": [
                "string",
                "Function"
            ]
        }
    },
    "data.tooltip.backgroundColor": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe background color of tooltip's floating layer.",
        "uiControl": {
            "default": "'rgba(50,50,50,0.7)'",
            "type": "Color"
        }
    },
    "data.tooltip.borderColor": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe border color of tooltip's floating layer.",
        "uiControl": {
            "default": "'#333'",
            "type": "color"
        }
    },
    "data.tooltip.borderWidth": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe border width of tooltip's floating layer.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.tooltip.padding": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\n\n<ExampleUIControlVector min=\"0\" dims=\"T,R,B,L\"  />\n\nThe floating layer of tooltip space around content. The unit is px. Default values for each position are 5. And they can be set to different values with left, right, top, and bottom.\n\nExamples:\n```js\n// Set padding to be 5\npadding: 5\n// Set the top and bottom paddings to be 5, and left and right paddings to be 10\npadding: [5, 10]\n// Set each of the four paddings seperately\npadding: [\n5,  // up\n10, // right\n5,  // down\n10, // left\n]\n```",
        "uiControl": {
            "default": "5",
            "type": "vector",
            "dims": "T,R,B,L"
        }
    },
    "data.tooltip.textStyle": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nThe text syle of tooltip's floating layer.",
        "uiControl": {
            "type": "Object"
        }
    },
    "data.tooltip.textStyle.color": {
        "desc": "text color.",
        "uiControl": {
            "default": "'#fff'",
            "type": "color"
        }
    },
    "data.tooltip.textStyle.fontStyle": {
        "desc": "font style.\n\nOptions are:\n+ `'normal'`\n+ `'italic'`\n+ `'oblique'`",
        "uiControl": {
            "default": "'normal'",
            "type": "enum",
            "options": "'normal','italic','oblique'"
        }
    },
    "data.tooltip.textStyle.fontWeight": {
        "desc": "font thick weight.\n\nOptions are:\n+ `'normal'`\n+ `'bold'`\n+ `'bolder'`\n+ `'lighter'`\n+ 100 | 200 | 300 | 400...",
        "uiControl": {
            "default": "'normal'",
            "type": "enum",
            "options": "'normal','bold','bolder','lighter'"
        }
    },
    "data.tooltip.textStyle.fontFamily": {
        "desc": "font family.\n\nCan also be 'serif' , 'monospace', ...",
        "uiControl": {
            "default": "'sans-serif'",
            "type": "enum",
            "options": "'sans-serif','serif','monospace','Arial','Courier New'"
        }
    },
    "data.tooltip.textStyle.fontSize": {
        "desc": "font size.",
        "uiControl": {
            "default": "14",
            "type": "number",
            "min": "1",
            "step": "1"
        }
    },
    "data.tooltip.textStyle.lineHeight": {
        "desc": "Line height of the text fragment.\n\n\n\nIf `lineHeight` is not set in `rich`, `lineHeight` in parent level will be used. For example:\n\n```js\n{\nlineHeight: 56,\nrich: {\na: {\n// `lineHeight` is not set, then it will be 56\n}\n}\n}\n```",
        "uiControl": {
            "default": "12",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.tooltip.textStyle.width": {
        "desc": "Width of text block.",
        "uiControl": {
            "default": "100",
            "type": "number",
            "min": "1",
            "max": "500",
            "step": "1"
        }
    },
    "data.tooltip.textStyle.height": {
        "desc": "Height of text block.",
        "uiControl": {
            "default": "50",
            "type": "number",
            "min": "1",
            "max": "500",
            "step": "1"
        }
    },
    "data.tooltip.textStyle.textBorderColor": {
        "desc": "Storke color of the text.",
        "uiControl": {
            "type": "color"
        }
    },
    "data.tooltip.textStyle.textBorderWidth": {
        "desc": "Storke line width of the text.",
        "uiControl": {
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.tooltip.textStyle.textBorderType": {
        "desc": "Stroke line type of the text.\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`textBorderDashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\ntextBorderType: [5, 10],\n\ntextBorderDashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "data.tooltip.textStyle.textBorderDashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`textBorderType`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "data.tooltip.textStyle.textShadowColor": {
        "desc": "Shadow color of the text itself.",
        "uiControl": {
            "default": "'transparent'",
            "type": "color"
        }
    },
    "data.tooltip.textStyle.textShadowBlur": {
        "desc": "Shadow blue of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "data.tooltip.textStyle.textShadowOffsetX": {
        "desc": "Shadow X offset of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.tooltip.textStyle.textShadowOffsetY": {
        "desc": "Shadow Y offset of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "data.tooltip.textStyle.overflow": {
        "desc": "Determine how to display the text when it's overflow. Available when `width` is set.\n\n+ `'truncate'` Truncate the text and trailing with `ellipsis`.\n+ `'break'` Break by word\n+ `'breakAll'` Break by character.",
        "uiControl": {
            "default": "'none'",
            "type": "enum",
            "options": "'truncate','break','breakAll'"
        }
    },
    "data.tooltip.textStyle.ellipsis": {
        "desc": "Ellipsis to be displayed when `overflow` is set to `truncate`.",
        "uiControl": {
            "default": "'...'",
            "type": "string"
        }
    },
    "data.tooltip.textStyle.lineOverflow": {
        "desc": "Determine how to display the text when it's overflow on height.\n\n+ `'truncate'` Truncate the overflow lines.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "data.tooltip.extraCssText": {
        "desc": "> **Notice：**series.data.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\nExtra CSS style for floating layer. The following is an example for adding shadow.\n\n```js\nextraCssText: 'box-shadow: 0 0 3px rgba(0, 0, 0, 0.3);'\n```",
        "uiControl": {
            "type": "text"
        }
    },
    "clip": {
        "desc": "> Since `v`\n\n\n\nIf clip the overflow on the coordinate system. Clip results varies between series:\n\n+ Scatter/EffectScatter：Ignore the symbols exceeds the coordinate system. Not clip the elements.\n+ Bar：Clip all the overflowed. With bar width kept.\n+ Line：Clip the overflowed line.\n+ Lines: Clip all the overflowed.\n+ Candlestick: Ignore the elements exceeds the coordinate system.\n+ Custom: Clip all the olverflowed.\n\nAll these series have default value `true` except custom series. Set it to `false` if you don't want to clip.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "zlevel": {
        "desc": "`zlevel` value of all graphical elements in custom series.\n\n`zlevel` is used to make layers with Canvas. Graphical elements with different `zlevel` values will be placed in different Canvases, which is a common optimization technique. We can put those frequently changed elements (like those with animations) to a separate `zlevel`. Notice that too many Canvases will increase memory cost, and should be used carefully on mobile phones to avoid crash.\n\nCanvases with bigger `zlevel` will be placed on Canvases with smaller `zlevel`.",
        "uiControl": {
            "default": "0",
            "type": "number"
        }
    },
    "z": {
        "desc": "`z` value of all graphical elements in custom series, which controls order of drawing graphical components. Components with smaller `z` values may be overwritten by those with larger `z` values.\n\n`z` has a lower priority to `zlevel`, and will not create new Canvas.",
        "uiControl": {
            "default": "2",
            "type": "number"
        }
    },
    "silent": {
        "desc": "Whether to ignore mouse events. Default value is false, for triggering and responding to mouse events.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "animation": {
        "desc": "Whether to enable animation.",
        "uiControl": {
            "default": "true",
            "type": "boolean"
        }
    },
    "animationThreshold": {
        "desc": "Whether to set graphic number threshold to animation. Animation will be disabled when graphic number is larger than threshold.",
        "uiControl": {
            "default": "2000",
            "type": "number"
        }
    },
    "animationDuration": {
        "desc": "Duration of the first animation, which supports callback function for different data to have different animation effect:\n\n```js\nanimationDuration: function (idx) {\n// delay for later data is larger\nreturn idx * 100;\n}\n```",
        "uiControl": {
            "default": "1000",
            "type": "number",
            "min": "0",
            "step": "20"
        }
    },
    "animationEasing": {
        "desc": "Easing method used for the first animation. Varied easing effects can be found at [easing effect example](line-easing).",
        "uiControl": {
            "default": "cubicOut",
            "type": "enum",
            "options": "'linear','quadraticIn','quadraticOut','quadraticInOut','cubicIn','cubicOut','cubicInOut','quarticIn','quarticOut','quarticInOut','quinticIn','quinticOut','quinticInOut','sinusoidalIn','sinusoidalOut','sinusoidalInOut','exponentialIn','exponentialOut','exponentialInOut','circularIn','circularOut','circularInOut','elasticIn','elasticOut','elasticInOut','backIn','backOut','backInOut','bounceIn','bounceOut','bounceInOut'"
        }
    },
    "animationDelay": {
        "desc": "Delay before updating the first animation, which supports callback function for different data to have different animation effect.\n\nFor example:\n```js\nanimationDelay: function (idx) {\n// delay for later data is larger\nreturn idx * 100;\n}\n```\n\nSee [this example](bar-animation-delay) for more information.",
        "uiControl": {
            "default": "0",
            "type": [
                "number",
                "Function"
            ]
        }
    },
    "animationDurationUpdate": {
        "desc": "Time for animation to complete, which supports callback function for different data to have different animation effect:\n\n```js\nanimationDurationUpdate: function (idx) {\n// delay for later data is larger\nreturn idx * 100;\n}\n```",
        "uiControl": {
            "default": "300",
            "type": "number",
            "min": "0",
            "step": "20"
        }
    },
    "animationEasingUpdate": {
        "desc": "Easing method used for animation.",
        "uiControl": {
            "default": "cubicOut",
            "type": "enum",
            "options": "'linear','quadraticIn','quadraticOut','quadraticInOut','cubicIn','cubicOut','cubicInOut','quarticIn','quarticOut','quarticInOut','quinticIn','quinticOut','quinticInOut','sinusoidalIn','sinusoidalOut','sinusoidalInOut','exponentialIn','exponentialOut','exponentialInOut','circularIn','circularOut','circularInOut','elasticIn','elasticOut','elasticInOut','backIn','backOut','backInOut','bounceIn','bounceOut','bounceInOut'"
        }
    },
    "animationDelayUpdate": {
        "desc": "Delay before updating animation, which supports callback function for different data to have different animation effects.\n\nFor example:\n```js\nanimationDelayUpdate: function (idx) {\n// delay for later data is larger\nreturn idx * 100;\n}\n```\n\nSee [this example](bar-animation-delay) for more information.",
        "uiControl": {
            "default": "0",
            "type": [
                "number",
                "Function"
            ]
        }
    },
    "universalTransition": {
        "desc": "Configuration related to universal transition animation.\n\nUniversal Transition provides the ability to morph between any series. With this feature enabled, each time `setOption`, transitions between series with the same `id` will be automatically associated with each other.\n\nOne-to-many or many-to-one animations such as drill-down, aggregation, etc. can also be achieved by specifying groups of data such as `encode.itemGroupId` or `dataGroupId`.\n\nThis can be enabled directly by configuring `universalTransition: true` in the series. It is also possible to provide an object for more detailed configuration.",
        "uiControl": {
            "type": "object"
        }
    },
    "universalTransition.enabled": {
        "desc": "Wheather to enable the universal transition animation.",
        "uiControl": {
            "default": "false",
            "type": "boolean"
        }
    },
    "universalTransition.seriesKey": {
        "desc": "The `seriesKey` determines how the series to be animated is associated, it defaults to the `id` of the series when not configured.\n\nUsually this is configured as a string, and transitions between series with the same `seriesKey` will be applied. It can also be configured as an array like the following.\n\n```js\nseriesKey: ['male', 'female']\n```\n\nConfiguring to an array means that all series specified by the array item will be merged into the current series when animating. For example, this configuration means that series with `id` or `seriesKey` of `'male'` and `'female'` will be merged into the current series.",
        "uiControl": {
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "universalTransition.divideShape": {
        "desc": "`divideShape` determines how the elements in the current series will split into multiple elements in a one-to-many or many-to-one animation. Currently supports\n\n+ `'split'` Split the shape into multiple shapes.\n+ `'clone'` Get multiple clones from the current element.\n\nFor better results, different series will have different configurations by default, for example, [scatter](~series-scatter) with smaller and more complex element uses `'clone'` by default, while more regular ones like bar charts default to `'split'`. You can set this to the desired splitting strategy according to the needs of your own scenario.",
        "uiControl": {
            "type": "string"
        }
    },
    "universalTransition.delay": {
        "desc": "```ts\n(index: number, count: number) => number\n```\n\nConfigure the animation delay for each shape in a one-to-many or many-to-one animation. Setting different animation delays can bring a more instereting animation. For example, the following code creates a staggered effect with a random delay for each shape.\n\n```js\ndelay: function (index, count) {\nreturn Math.random() * 1000;\n}\n```",
        "uiControl": {
            "type": "Function"
        }
    },
    "tooltip": {
        "desc": "tooltip settings in this series.",
        "uiControl": {
            "type": "*"
        }
    },
    "tooltip.position": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe position of the tooltip's floating layer, which would follow the position of mouse by default.\n\nOptions:\n\n+ `Array`\n\nDisplay the position of tooltip's floating layer through array, which supports absolute position and relative percentage.\n\nExample:\n\n```js\n// absolute position, which is 10px to the left side and 10px to the top side of the container\nposition: [10, 10]\n// relative position, in the exact center of the container\nposition: ['50%', '50%']\n```\n\n+ `Function`\n\nCallback function in the following form:\n```js\n(point: Array, params: Object|Array.<Object>, dom: HTMLDomElement, rect: Object, size: Object) => Array\n```\n\n**Parameters:**<br>\npoint: Mouse position.<br>\nparam: The same as formatter.<br>\ndom: The DOM object of tooltip.<br>\nrect: It is valid only when mouse is on graphic elements, which stands for a bounding box with `x`, `y`, `width`, and `height`.<br>\nsize: The size of dom echarts container. For example: `{contentSize: [width, height], viewSize: [width, height]}`. <br>\n\n**Return:**<br>\nReturn value is an array standing for tooltip position, which can be absolute pixels, or relative percentage.<br>\nOr can be an object, like `{left: 10, top: 30}`, or `{right: '20%', bottom: 40}`.<br>\n\nFor example:\n```js\nposition: function (point, params, dom, rect, size) {\n// fixed at top\nreturn [point[0], '10%'];\n}\n```\nOr:\n```js\nposition: function (pos, params, dom, rect, size) {\n// tooltip will be fixed on the right if mouse hovering on the left,\n// and on the left if hovering on the right.\nvar obj = {top: 60};\nobj[['left', 'right'][+(pos[0] < size.viewSize[0] / 2)]] = 5;\nreturn obj;\n}\n```\n\n+ `'inside'`\n\nCenter position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'top'`\n\nTop position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'left'`\n\nLeft position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'right'`\n\nRight position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.\n\n+ `'bottom'`\n\nBottom position of the graphic element where the mouse is in, which is only valid when [trigger](~tooltip.trigger) is `'item'`.",
        "uiControl": {
            "type": [
                "string",
                "Array"
            ]
        }
    },
    "tooltip.formatter": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe content formatter of tooltip's floating layer which supports string template and callback function.\n\n**1. String template**\n\nThe template variables are `{a}`, `{b}`, `{c}`, `{d}` and `{e}`, which stands for series name, data name and data value and ect. When [trigger](~tooltip.trigger) is set to be `'axis'`, there may be data from multiple series. In this time, series index can be refered as `{a0}`, `{a1}`, or `{a2}`.\n\n`{a}`, `{b}`, `{c}`, `{d}` have different meanings for different series types:\n\n+ Line (area) charts, bar (column) charts, K charts: `{a}` for series name, `{b}` for category name, `{c}` for data value, `{d}` for none;\n\n+ Scatter (bubble) charts: `{a}` for series name, `{b}` for data name, `{c}` for data value, `{d}` for none;\n\n+ Map: `{a}` for series name, `{b}` for area name, `{c}` for merging data, `{d}` for none;\n\n+ Pie charts, gauge charts, funnel charts: `{a}` for series name, `{b}` for data item name, `{c}` for data value, `{d}` for percentage.\n\n**Example: **\n```js\nformatter: '{b0}: {c0}<br />{b1}: {c1}'\n```\n\n\n**2. Callback function**\n\nThe format of callback function:\n\n```js\n(params: Object|Array, ticket: string, callback: (ticket: string, html: string)) => string | HTMLElement | HTMLElement[]\n```\n\nThe first parameter `params` is the data that the formatter needs. Its format is shown as follows:\n\n\n\n```js\n{\ncomponentType: 'series',\n// Series type\nseriesType: string,\n// Series index in option.series\nseriesIndex: number,\n// Series name\nseriesName: string,\n// Data name, or category name\nname: string,\n// Data index in input data array\ndataIndex: number,\n// Original data as input\ndata: Object,\n// Value of data. In most series it is the same as data.\n// But in some series it is some part of the data (e.g., in map, radar)\nvalue: number|Array|Object,\n// encoding info of coordinate system\n// Key: coord, like ('x' 'y' 'radius' 'angle')\n// value: Must be an array, not null/undefined. Contain dimension indices, like:\n// {\n//     x: [2] // values on dimension index 2 are mapped to x axis.\n//     y: [0] // values on dimension index 0 are mapped to y axis.\n// }\nencode: Object,\n// dimension names list\ndimensionNames: Array<String>,\n// data dimension index, for example 0 or 1 or 2 ...\n// Only work in `radar` series.\ndimensionIndex: number,\n// Color of data\ncolor: string,\n\n{{ for:  as ,  }}{{ /for }}\n\n}\n```\n\nNote: the usage of encode and dimensionNames can be:\n\nIf data is:\n```js\ndataset: {\nsource: [\n['Matcha Latte', 43.3, 85.8, 93.7],\n['Milk Tea', 83.1, 73.4, 55.1],\n['Cheese Cocoa', 86.4, 65.2, 82.5],\n['Walnut Brownie', 72.4, 53.9, 39.1]\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.encode.y[0]]\n```\n\nIf data is:\n```js\ndataset: {\ndimensions: ['product', '2015', '2016', '2017'],\nsource: [\n{product: 'Matcha Latte', '2015': 43.3, '2016': 85.8, '2017': 93.7},\n{product: 'Milk Tea', '2015': 83.1, '2016': 73.4, '2017': 55.1},\n{product: 'Cheese Cocoa', '2015': 86.4, '2016': 65.2, '2017': 82.5},\n{product: 'Walnut Brownie', '2015': 72.4, '2016': 53.9, '2017': 39.1}\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.dimensionNames[params.encode.y[0]]]\n```\n\n\n\nWhen [trigger](~tooltip.trigger) is `'axis'`, or when tooltip is triggered by [axisPointer](~xAxis.axisPointer), `params` is the data array of multiple series. The content of each item of the array is the same as above. Besides,\n\n\n\n```js\n{\ncomponentType: 'series',\n// Series type\nseriesType: string,\n// Series index in option.series\nseriesIndex: number,\n// Series name\nseriesName: string,\n// Data name, or category name\nname: string,\n// Data index in input data array\ndataIndex: number,\n// Original data as input\ndata: Object,\n// Value of data. In most series it is the same as data.\n// But in some series it is some part of the data (e.g., in map, radar)\nvalue: number|Array|Object,\n// encoding info of coordinate system\n// Key: coord, like ('x' 'y' 'radius' 'angle')\n// value: Must be an array, not null/undefined. Contain dimension indices, like:\n// {\n//     x: [2] // values on dimension index 2 are mapped to x axis.\n//     y: [0] // values on dimension index 0 are mapped to y axis.\n// }\nencode: Object,\n// dimension names list\ndimensionNames: Array<String>,\n// data dimension index, for example 0 or 1 or 2 ...\n// Only work in `radar` series.\ndimensionIndex: number,\n// Color of data\ncolor: string,\n\n{{ for:  as ,  }}{{ /for }}\n\n}\n```\n\nNote: the usage of encode and dimensionNames can be:\n\nIf data is:\n```js\ndataset: {\nsource: [\n['Matcha Latte', 43.3, 85.8, 93.7],\n['Milk Tea', 83.1, 73.4, 55.1],\n['Cheese Cocoa', 86.4, 65.2, 82.5],\n['Walnut Brownie', 72.4, 53.9, 39.1]\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.encode.y[0]]\n```\n\nIf data is:\n```js\ndataset: {\ndimensions: ['product', '2015', '2016', '2017'],\nsource: [\n{product: 'Matcha Latte', '2015': 43.3, '2016': 85.8, '2017': 93.7},\n{product: 'Milk Tea', '2015': 83.1, '2016': 73.4, '2017': 55.1},\n{product: 'Cheese Cocoa', '2015': 86.4, '2016': 65.2, '2017': 82.5},\n{product: 'Walnut Brownie', '2015': 72.4, '2016': 53.9, '2017': 39.1}\n]\n}\n```\nWe can get values that corresponding to y axis by:\n```js\nparams.value[params.dimensionNames[params.encode.y[0]]]\n```\n\n\n\n**Note: **Using array to present all the parameters in ECharts 2.x is not supported anymore.\n\nThe second parameter `ticket` is the asynchronous callback flag which should be used along with the third parameter `callback` when it is used.\n\nThe third parameter `callback` is asynchronous callback. When the content of tooltip is acquired asynchronously, `ticket` and `htm` as introduced above can be used to update tooltip with callback.\n\nExample:\n```js\nformatter: function (params, ticket, callback) {\n$.get('detail?name=' + params.name, function (content) {\ncallback(ticket, toHTML(content));\n});\nreturn 'Loading';\n}\n```",
        "uiControl": {
            "type": [
                "string",
                "Function"
            ]
        }
    },
    "tooltip.backgroundColor": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe background color of tooltip's floating layer.",
        "uiControl": {
            "default": "'rgba(50,50,50,0.7)'",
            "type": "Color"
        }
    },
    "tooltip.borderColor": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe border color of tooltip's floating layer.",
        "uiControl": {
            "default": "'#333'",
            "type": "color"
        }
    },
    "tooltip.borderWidth": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe border width of tooltip's floating layer.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "tooltip.padding": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\n\n\n<ExampleUIControlVector min=\"0\" dims=\"T,R,B,L\"  />\n\nThe floating layer of tooltip space around content. The unit is px. Default values for each position are 5. And they can be set to different values with left, right, top, and bottom.\n\nExamples:\n```js\n// Set padding to be 5\npadding: 5\n// Set the top and bottom paddings to be 5, and left and right paddings to be 10\npadding: [5, 10]\n// Set each of the four paddings seperately\npadding: [\n5,  // up\n10, // right\n5,  // down\n10, // left\n]\n```",
        "uiControl": {
            "default": "5",
            "type": "vector",
            "dims": "T,R,B,L"
        }
    },
    "tooltip.textStyle": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nThe text syle of tooltip's floating layer.",
        "uiControl": {
            "type": "Object"
        }
    },
    "tooltip.textStyle.color": {
        "desc": "text color.",
        "uiControl": {
            "default": "'#fff'",
            "type": "color"
        }
    },
    "tooltip.textStyle.fontStyle": {
        "desc": "font style.\n\nOptions are:\n+ `'normal'`\n+ `'italic'`\n+ `'oblique'`",
        "uiControl": {
            "default": "'normal'",
            "type": "enum",
            "options": "'normal','italic','oblique'"
        }
    },
    "tooltip.textStyle.fontWeight": {
        "desc": "font thick weight.\n\nOptions are:\n+ `'normal'`\n+ `'bold'`\n+ `'bolder'`\n+ `'lighter'`\n+ 100 | 200 | 300 | 400...",
        "uiControl": {
            "default": "'normal'",
            "type": "enum",
            "options": "'normal','bold','bolder','lighter'"
        }
    },
    "tooltip.textStyle.fontFamily": {
        "desc": "font family.\n\nCan also be 'serif' , 'monospace', ...",
        "uiControl": {
            "default": "'sans-serif'",
            "type": "enum",
            "options": "'sans-serif','serif','monospace','Arial','Courier New'"
        }
    },
    "tooltip.textStyle.fontSize": {
        "desc": "font size.",
        "uiControl": {
            "default": "14",
            "type": "number",
            "min": "1",
            "step": "1"
        }
    },
    "tooltip.textStyle.lineHeight": {
        "desc": "Line height of the text fragment.\n\n\n\nIf `lineHeight` is not set in `rich`, `lineHeight` in parent level will be used. For example:\n\n```js\n{\nlineHeight: 56,\nrich: {\na: {\n// `lineHeight` is not set, then it will be 56\n}\n}\n}\n```",
        "uiControl": {
            "default": "12",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "tooltip.textStyle.width": {
        "desc": "Width of text block.",
        "uiControl": {
            "default": "100",
            "type": "number",
            "min": "1",
            "max": "500",
            "step": "1"
        }
    },
    "tooltip.textStyle.height": {
        "desc": "Height of text block.",
        "uiControl": {
            "default": "50",
            "type": "number",
            "min": "1",
            "max": "500",
            "step": "1"
        }
    },
    "tooltip.textStyle.textBorderColor": {
        "desc": "Storke color of the text.",
        "uiControl": {
            "type": "color"
        }
    },
    "tooltip.textStyle.textBorderWidth": {
        "desc": "Storke line width of the text.",
        "uiControl": {
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "tooltip.textStyle.textBorderType": {
        "desc": "Stroke line type of the text.\n\n\nPossible values are:\n+ `'solid'`\n+ `'dashed'`\n+ `'dotted'`\n\nSince `v5.0.0`, it can also be a number or a number array to specify the [dash array](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/stroke-dasharray) of the line. With\n`textBorderDashOffset`\n, we can make the line style more flexible.\n\nFor example：\n```js\n{\n\ntextBorderType: [5, 10],\n\ntextBorderDashOffset: 5\n}\n```",
        "uiControl": {
            "default": "'solid'",
            "type": "enum",
            "options": "'solid','dashed','dotted'"
        }
    },
    "tooltip.textStyle.textBorderDashOffset": {
        "desc": "> Since `v5.0.0`\n\n\n\n\n\nTo set the line dash offset. With\n`textBorderType`\n, we can make the line style more flexible.\n\nRefer to MDN [lineDashOffset](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/lineDashOffset) for more details.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "1"
        }
    },
    "tooltip.textStyle.textShadowColor": {
        "desc": "Shadow color of the text itself.",
        "uiControl": {
            "default": "'transparent'",
            "type": "color"
        }
    },
    "tooltip.textStyle.textShadowBlur": {
        "desc": "Shadow blue of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "min": "0",
            "step": "0.5"
        }
    },
    "tooltip.textStyle.textShadowOffsetX": {
        "desc": "Shadow X offset of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "tooltip.textStyle.textShadowOffsetY": {
        "desc": "Shadow Y offset of the text itself.",
        "uiControl": {
            "default": "0",
            "type": "number",
            "step": "0.5"
        }
    },
    "tooltip.textStyle.overflow": {
        "desc": "Determine how to display the text when it's overflow. Available when `width` is set.\n\n+ `'truncate'` Truncate the text and trailing with `ellipsis`.\n+ `'break'` Break by word\n+ `'breakAll'` Break by character.",
        "uiControl": {
            "default": "'none'",
            "type": "enum",
            "options": "'truncate','break','breakAll'"
        }
    },
    "tooltip.textStyle.ellipsis": {
        "desc": "Ellipsis to be displayed when `overflow` is set to `truncate`.",
        "uiControl": {
            "default": "'...'",
            "type": "string"
        }
    },
    "tooltip.textStyle.lineOverflow": {
        "desc": "Determine how to display the text when it's overflow on height.\n\n+ `'truncate'` Truncate the overflow lines.",
        "uiControl": {
            "default": "'none'",
            "type": "string"
        }
    },
    "tooltip.extraCssText": {
        "desc": "<br>\n> **Notice：**series.tooltip only works when [tooltip.trigger](~tooltip.trigger) is `'item'`.<br>\n\n\n\n\n\n\n\nExtra CSS style for floating layer. The following is an example for adding shadow.\n\n```js\nextraCssText: 'box-shadow: 0 0 3px rgba(0, 0, 0, 0.3);'\n```",
        "uiControl": {
            "type": "text"
        }
    }
}